export type CCDifficulty = "beginner" | "intermediate" | "advanced-beginner";
export type CCBlankKind = "inline" | "block";

export interface CCBlank {
  id: string;
  kind: CCBlankKind;
  acceptableAnswers: string[];
}

export interface CodeCompletionQuestion {
  id: string;
  title: string;
  topic: string;
  difficulty: CCDifficulty;
  scenario: string;
  task: string;
  starterCode: string;
  blanks: CCBlank[];
  solutionCode: string;
  testCases: string[];
  explanation: string;
  commonMistake: string;
  hints: string[];
  tags: string[];
}

export const ccTopics: { id: string; label: string }[] = [
  { id: "program-structure", label: "Program Structure" },
  { id: "variables", label: "Variables" },
  { id: "data-types", label: "Data Types" },
  { id: "io", label: "Input & Output" },
  { id: "arithmetic", label: "Arithmetic Operators" },
  { id: "comparison", label: "Comparison Operators" },
  { id: "logical", label: "Logical Operators" },
  { id: "if-else", label: "if / else" },
  { id: "switch", label: "switch" },
  { id: "loops", label: "Loops" },
  { id: "arrays", label: "Arrays" },
  { id: "strings", label: "Strings" },
  { id: "functions", label: "Functions" },
  { id: "references", label: "References" },
  { id: "pointers", label: "Pointers" },
  { id: "dynamic-memory", label: "Dynamic Memory" },
  { id: "structs", label: "Structs" },
  { id: "classes", label: "Classes / OOP Basics" },
  { id: "type-conversion", label: "Type Conversion" },
];

export const codeCompletionBank: CodeCompletionQuestion[] = [
  // ================= A. Program Structure (7) =================
  {
    id: "cc-ps-1",
    title: "Your first program",
    topic: "program-structure",
    difficulty: "beginner",
    scenario: "You're writing the smallest possible C++ program to confirm your compiler is set up correctly.",
    task: "Complete the header include so the program can use cout.",
    starterCode: "/*BLANK*/\nusing namespace std;\n\nint main() {\n    cout << \"Hello, bank!\";\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["#include <iostream>", "#include<iostream>"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"Hello, bank!\";\n    return 0;\n}",
    testCases: ["Program compiles and prints: Hello, bank!"],
    explanation: "cout lives in the <iostream> header, so any file that uses it must #include <iostream> at the top. Without this line, the compiler has never heard of cout and will refuse to compile with an 'undeclared identifier' error.",
    commonMistake: "Forgetting the include entirely, or misspelling it as 'Iostream' or '<iostream.h>' (the old pre-standard header name, which modern compilers reject).",
    hints: ["Which header declares cout and cin?", "The header name is all lowercase, with no file extension."],
    tags: ["include", "iostream", "syntax"],
  },
  {
    id: "cc-ps-2",
    title: "Returning success",
    topic: "program-structure",
    difficulty: "beginner",
    scenario: "A grading script checks the exit code of every student's compiled program to confirm it finished without crashing.",
    task: "Complete main's final statement so the program reports success to the operating system.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"Balance updated\";\n    /*BLANK*/\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["return 0;", "return 0"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"Balance updated\";\n    return 0;\n}",
    testCases: ["Program exits with status code 0 (success)."],
    explanation: "main() is declared to return an int, and by convention returning 0 tells the operating system the program finished successfully; any nonzero value signals an error occurred. This is why almost every introductory main() ends with return 0;.",
    commonMistake: "Leaving main() without any return statement at all, which some compilers allow (implicitly returning 0) but which is not something you should rely on — always write it explicitly.",
    tags: ["main", "return", "exit-code"],
    hints: ["main() was declared to return what type?", "Zero conventionally means 'no error occurred'."],
  },
  {
    id: "cc-ps-3",
    title: "Avoiding std:: everywhere",
    topic: "program-structure",
    difficulty: "beginner",
    scenario: "A teammate's code compiles, but every single line is prefixed with std:: and it's hard to read.",
    task: "Complete the directive that lets the program use cout and cin without the std:: prefix.",
    starterCode: "#include <iostream>\n/*BLANK*/\n\nint main() {\n    cout << \"Welcome to MobileWallet\";\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["using namespace std;"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"Welcome to MobileWallet\";\n    return 0;\n}",
    testCases: ["Program compiles and prints: Welcome to MobileWallet"],
    explanation: "cout, cin, and endl all actually live inside the std namespace, as std::cout, std::cin, and std::endl. The directive using namespace std; tells the compiler to look inside that namespace automatically, so you can write cout instead of std::cout.",
    commonMistake: "Writing 'using std namespace;' (wrong word order) or forgetting the trailing semicolon.",
    hints: ["This is a 'using' directive, naming a namespace.", "The keyword order is: using namespace <name>;"],
    tags: ["namespace", "std", "syntax"],
  },
  {
    id: "cc-ps-4",
    title: "Printing on one line vs several",
    topic: "program-structure",
    difficulty: "beginner",
    scenario: "A receipt printer for a shop needs to show the item name and price on the same output line.",
    task: "Complete the statement using the insertion operator so both pieces print together, one after another.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    string item = \"Coffee\";\n    double price = 12.50;\n    cout << item /*BLANK*/ price;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["<<"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    string item = \"Coffee\";\n    double price = 12.50;\n    cout << item << price;\n    return 0;\n}",
    testCases: ["Output: Coffee12.5"],
    explanation: "The << (insertion) operator can be chained — each << sends the next value to cout in sequence, so cout << item << price prints item immediately followed by price, with nothing in between unless you add it yourself (like a space or a newline).",
    commonMistake: "Using a comma instead of another <<, which is not valid syntax for chaining output in C++ (unlike some other languages' print functions).",
    hints: ["cout uses the same operator every time you send it something new.", "It's the two-less-than-signs operator: <<"],
    tags: ["cout", "insertion-operator", "output"],
  },
  {
    id: "cc-ps-5",
    title: "A comment explaining intent",
    topic: "program-structure",
    difficulty: "beginner",
    scenario: "Your lecturer asks you to leave a one-line note above a tricky calculation so the next reader understands its purpose.",
    task: "Complete the single-line comment marker.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    /*BLANK*/ 2% transaction fee applied below\n    double fee = 1000 * 0.02;\n    cout << fee;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["//"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    // 2% transaction fee applied below\n    double fee = 1000 * 0.02;\n    cout << fee;\n    return 0;\n}",
    testCases: ["Program compiles (comment is ignored by the compiler); output: 20"],
    explanation: "// starts a single-line comment — everything from // to the end of that line is ignored by the compiler. Comments don't affect what the program does; they exist purely to explain the code to human readers.",
    commonMistake: "Using /* without a closing */, which turns the comment into a block comment that silently swallows all the following code until a */ is found somewhere later.",
    hints: ["A single-line comment starts with two of the same punctuation character.", "It's two forward slashes."],
    tags: ["comments", "syntax"],
  },
  {
    id: "cc-ps-6",
    title: "Ending every statement",
    topic: "program-structure",
    difficulty: "beginner",
    scenario: "A student's payroll program fails to compile with a confusing error pointing at the line after a simple print statement.",
    task: "Complete the missing punctuation that terminates the statement.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"Payslip generated\"/*BLANK*/\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: [";"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"Payslip generated\";\n    return 0;\n}",
    testCases: ["Program compiles and prints: Payslip generated"],
    explanation: "Every C++ statement must end with a semicolon. It's how the compiler knows where one instruction ends and the next begins — unlike some languages, C++ does not use line breaks for this.",
    commonMistake: "Forgetting the semicolon, which usually produces a compiler error pointing at the *next* line rather than the missing one, confusing beginners about where the real problem is.",
    hints: ["What single punctuation mark ends almost every C++ statement?", "It's the semicolon: ;"],
    tags: ["semicolon", "syntax", "statements"],
  },
  {
    id: "cc-ps-7",
    title: "Where execution begins",
    topic: "program-structure",
    difficulty: "beginner",
    scenario: "A new C++ file has several functions defined, but nothing runs when the program is executed because the entry point is missing.",
    task: "Complete the function signature that the operating system looks for and calls first.",
    starterCode: "#include <iostream>\nusing namespace std;\n\n/*BLANK*/ {\n    cout << \"ATM system starting...\";\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["int main()", "int main( )"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"ATM system starting...\";\n    return 0;\n}",
    testCases: ["Program compiles and runs; prints: ATM system starting..."],
    explanation: "Every C++ program must have exactly one function named main — this is the entry point the operating system calls when the program starts. It's declared to return an int (the exit status) and, in this simple form, takes no parameters.",
    commonMistake: "Misspelling it as 'Main' (capitalized) or 'void main()' — C++ requires the return type int, not void, for a standard-conforming main.",
    hints: ["This function's name is exactly three lowercase letters.", "Its return type is int, and its name is main."],
    tags: ["main", "entry-point", "syntax"],
  },

  // ================= B. Variables (8) =================
  {
    id: "cc-var-1",
    title: "Declaring an account balance",
    topic: "variables",
    difficulty: "beginner",
    scenario: "You're building a simple bank account tracker and need to store a customer's starting balance.",
    task: "Complete the declaration so balance starts at 500.75.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double /*BLANK*/;\n    cout << balance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["balance = 500.75"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 500.75;\n    cout << balance;\n    return 0;\n}",
    testCases: ["Output: 500.75"],
    explanation: "Declaring a variable with an initial value combines two steps in one line: reserving memory of the right type (double, here) and giving it a starting value with =. After this line, balance can be read or changed anywhere below it in the same scope.",
    commonMistake: "Declaring the variable without initializing it (double balance;) and then trying to print it before ever assigning a value — this reads garbage/undefined memory.",
    hints: ["You need a name for the variable and a value assigned with =.", "double balance = 500.75;"],
    tags: ["declaration", "initialization", "double"],
  },
  {
    id: "cc-var-2",
    title: "Reassigning after a deposit",
    topic: "variables",
    difficulty: "beginner",
    scenario: "A customer deposits 200 into their wallet, which currently holds 300.",
    task: "Complete the statement that updates walletBalance to reflect the deposit.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double walletBalance = 300;\n    double deposit = 200;\n    /*BLANK*/;\n    cout << walletBalance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["walletBalance = walletBalance + deposit", "walletBalance += deposit"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double walletBalance = 300;\n    double deposit = 200;\n    walletBalance = walletBalance + deposit;\n    cout << walletBalance;\n    return 0;\n}",
    testCases: ["300 deposit 200 -> 500"],
    explanation: "Reassignment (using = without a type in front) replaces a variable's current value with a new one — it does not create a second variable. Here the new value is computed from the old value plus the deposit, which is exactly the pattern used to update running totals.",
    commonMistake: "Writing 'double walletBalance = walletBalance + deposit;' — re-declaring the type creates a brand-new, separate variable instead of updating the existing one, and would actually be a compile error here since walletBalance is already declared in this scope.",
    hints: ["You want to update the existing variable, not declare a new one.", "walletBalance = walletBalance + deposit;"],
    tags: ["reassignment", "variables", "arithmetic"],
  },
  {
    id: "cc-var-3",
    title: "A constant interest rate",
    topic: "variables",
    difficulty: "beginner",
    scenario: "A savings account applies a fixed 3% annual interest rate that should never be accidentally changed elsewhere in the program.",
    task: "Complete the declaration so interestRate cannot be reassigned later.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    /*BLANK*/ double interestRate = 0.03;\n    cout << interestRate;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["const"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    const double interestRate = 0.03;\n    cout << interestRate;\n    return 0;\n}",
    testCases: ["Output: 0.03; attempting interestRate = 0.05; later would fail to compile"],
    explanation: "The const keyword marks a variable as read-only after initialization — the compiler will reject any later attempt to reassign it. This is exactly the right tool for values like a fixed interest rate that should never change during the program's run.",
    commonMistake: "Declaring it without const and relying on 'just not changing it' by convention — nothing then stops another part of the code (or a future edit) from accidentally overwriting it.",
    hints: ["You need a keyword that prevents reassignment after initialization.", "It's the keyword const, placed before the type."],
    tags: ["const", "constants", "variables"],
  },
  {
    id: "cc-var-4",
    title: "Multiple variables, one purpose",
    topic: "variables",
    difficulty: "beginner",
    scenario: "A student registration form needs to store three exam scores for the same student.",
    task: "Complete the declaration line so all three scores are declared as ints in one statement.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int /*BLANK*/;\n    cout << score1 << score2 << score3;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["score1 = 70, score2 = 85, score3 = 90", "score1=70, score2=85, score3=90"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int score1 = 70, score2 = 85, score3 = 90;\n    cout << score1 << score2 << score3;\n    return 0;\n}",
    testCases: ["Output: 708590"],
    explanation: "Multiple variables of the same type can be declared in a single statement by separating them with commas, each with its own initializer. This is purely a convenience — it behaves identically to three separate declaration lines.",
    commonMistake: "Repeating the type keyword for each variable within the same statement (int score1 = 70, int score2 = 85, ...) — once int appears at the start, it applies to the whole comma-separated list; repeating it is a syntax error.",
    hints: ["Separate each name=value pair with a comma, and only write 'int' once.", "int score1 = 70, score2 = 85, score3 = 90;"],
    tags: ["multiple-declaration", "variables", "int"],
  },
  {
    id: "cc-var-5",
    title: "A meaningful name",
    topic: "variables",
    difficulty: "beginner",
    scenario: "Your teammate named a variable 'x' to store a customer's age, and you can't tell what it means three lines later.",
    task: "Complete the declaration with a clear, descriptive variable name instead of 'x'.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int /*BLANK*/ = 25;\n    if (customerAge >= 18) cout << \"Eligible\";\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["customerAge"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int customerAge = 25;\n    if (customerAge >= 18) cout << \"Eligible\";\n    return 0;\n}",
    testCases: ["Program compiles because the declared name matches the name used later (customerAge)."],
    explanation: "Variable names should describe what they hold — customerAge is immediately understandable, while x forces every reader to trace back through the code to figure out its purpose. Here the name must also literally match what's used later in the if condition, or the code won't compile at all.",
    commonMistake: "Naming it something close but not exact (e.g., customer_age or CustomerAge) — C++ variable names are case-sensitive and must match exactly everywhere they're used.",
    hints: ["The name used later in the if-condition tells you exactly what this variable must be called.", "It's customerAge — matching the condition below exactly, including capitalization."],
    tags: ["naming", "readability", "variables"],
  },
  {
    id: "cc-var-6",
    title: "Declaring without a value yet",
    topic: "variables",
    difficulty: "intermediate",
    scenario: "A program needs to declare a variable to hold a transaction total, but the actual amount is only known after reading user input further down.",
    task: "Complete the declaration so total is declared with the right type but no initial value yet.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double /*BLANK*/;\n    cin >> total;\n    cout << total;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["total"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double total;\n    cin >> total;\n    cout << total;\n    return 0;\n}",
    testCases: ["User enters 250.5 -> program prints 250.5"],
    explanation: "A variable can be declared without an initial value when you plan to fill it in immediately afterward (here, via cin). The declaration reserves the memory and gives it a type; cin >> total then supplies the actual value before it's ever read.",
    commonMistake: "Trying to use total in an expression (like printing it) before cin >> total has actually run — an uninitialized double holds unpredictable garbage until something assigns it a real value.",
    hints: ["You only need the type and the name — no = value needed here, since cin will supply it.", "double total;"],
    tags: ["declaration", "cin", "uninitialized"],
  },
  {
    id: "cc-var-7",
    title: "Boolean flag for account status",
    topic: "variables",
    difficulty: "beginner",
    scenario: "A banking system needs to track whether an account is currently active or has been suspended.",
    task: "Complete the declaration so isActive correctly stores a true/false state.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool /*BLANK*/;\n    cout << isActive;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["isActive = true"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool isActive = true;\n    cout << isActive;\n    return 0;\n}",
    testCases: ["Output: 1 (C++ prints bool true as 1 by default)"],
    explanation: "bool is the type for values that are only ever true or false — perfect for a status flag like whether an account is active. By default, cout prints true as 1 and false as 0 unless you explicitly ask for the word form with cout << boolalpha.",
    commonMistake: "Storing status as an int (0 or 1) or a string (\"active\") instead of bool — this works but loses the compiler's guarantee that the variable can only ever hold one of exactly two values.",
    hints: ["This is a true/false value, so it needs the bool type.", "bool isActive = true;"],
    tags: ["bool", "flags", "variables"],
  },
  {
    id: "cc-var-8",
    title: "Naming rules — what's not allowed",
    topic: "variables",
    difficulty: "intermediate",
    scenario: "A student tries to name a variable '2ndAccount' and the code fails to compile.",
    task: "Complete the corrected variable name, which must not start with a digit.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int /*BLANK*/ = 2001;\n    cout << secondAccount;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["secondAccount"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int secondAccount = 2001;\n    cout << secondAccount;\n    return 0;\n}",
    testCases: ["Program compiles and prints: 2001"],
    explanation: "C++ variable names must start with a letter or underscore, never a digit — the compiler needs to be able to tell instantly whether a token is a number literal or an identifier, and allowing digits first would make that ambiguous. Renaming 2ndAccount to secondAccount fixes this and is also read later in the program, so the name must match exactly.",
    commonMistake: "Assuming any character can start a variable name as in some other languages — C++'s rule (letter or underscore first, then letters/digits/underscores) is stricter than that.",
    hints: ["Variable names can't begin with a digit — spell the number out as a word instead.", "secondAccount"],
    tags: ["naming-rules", "syntax", "variables"],
  },

  // ================= C. Data Types (10) =================
  {
    id: "cc-dt-1",
    title: "Storing a customer's age",
    topic: "data-types",
    difficulty: "beginner",
    scenario: "A registration form needs to store a customer's age as a whole number.",
    task: "Complete the type declaration for age.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    /*BLANK*/ age = 34;\n    cout << age;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["int"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int age = 34;\n    cout << age;\n    return 0;\n}",
    testCases: ["Output: 34"],
    explanation: "Age is naturally a whole number, so int is the right type — there's no fractional part to represent, and int is the standard choice for counting-style values.",
    commonMistake: "Using double for values that are always whole numbers (like age or a count of items) — it works, but it's the wrong tool and can introduce unnecessary floating-point quirks later.",
    hints: ["Ages are always whole numbers, never fractional.", "The whole-number type in C++ is int."],
    tags: ["int", "data-types"],
  },
  {
    id: "cc-dt-2",
    title: "Storing an interest rate precisely",
    topic: "data-types",
    difficulty: "beginner",
    scenario: "A loan calculator needs to store an interest rate of 4.75% with enough precision for accurate financial math.",
    task: "Complete the type declaration for rate, choosing the type with more precision than float.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    /*BLANK*/ rate = 0.0475;\n    cout << rate;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["double"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double rate = 0.0475;\n    cout << rate;\n    return 0;\n}",
    testCases: ["Output: 0.0475"],
    explanation: "double stores floating-point numbers with roughly twice the precision of float (about 15-16 significant decimal digits versus 6-7), which matters for financial calculations where small rounding errors can compound. In modern C++, double is the default, safe choice for any decimal value unless you have a specific reason to save memory with float.",
    commonMistake: "Using float out of habit — it compiles and often looks fine for small examples, but accumulates more rounding error over repeated calculations, which is exactly the kind of bug that's dangerous in financial software.",
    hints: ["You need a decimal type with more precision than float.", "It's double."],
    tags: ["double", "precision", "data-types"],
  },
  {
    id: "cc-dt-3",
    title: "A single character grade",
    topic: "data-types",
    difficulty: "beginner",
    scenario: "A grading system assigns each student a single letter grade: A, B, C, D, or F.",
    task: "Complete the declaration so grade can hold exactly one character.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    /*BLANK*/ grade = 'A';\n    cout << grade;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["char"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    char grade = 'A';\n    cout << grade;\n    return 0;\n}",
    testCases: ["Output: A"],
    explanation: "char stores exactly one character, written with single quotes ('A'), not double quotes (\"A\", which would make it a string). It's the right type whenever a value is inherently a single letter, digit character, or symbol.",
    commonMistake: "Writing \"A\" (double quotes) instead of 'A' (single quotes) — double quotes create a string literal, which cannot be assigned directly to a char variable.",
    hints: ["A single letter, in single quotes, needs a specific one-character type.", "It's char."],
    tags: ["char", "data-types"],
  },
  {
    id: "cc-dt-4",
    title: "Yes/no eligibility flag",
    topic: "data-types",
    difficulty: "beginner",
    scenario: "A loan eligibility checker needs to store whether an applicant qualifies — a simple yes-or-no result.",
    task: "Complete the type declaration for isEligible.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    /*BLANK*/ isEligible = false;\n    cout << isEligible;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["bool"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool isEligible = false;\n    cout << isEligible;\n    return 0;\n}",
    testCases: ["Output: 0"],
    explanation: "bool is the type built specifically for two-state, true-or-false values — exactly what a yes/no eligibility result is. It only ever holds true or false internally (printed as 1 or 0 by default).",
    commonMistake: "Using an int with 0 and 1 by convention instead of bool — this works but doesn't communicate intent as clearly and allows accidentally storing other numbers like 2 or -1, which bool cannot do.",
    hints: ["This is a two-state true/false value.", "bool isEligible = false;"],
    tags: ["bool", "data-types"],
  },
  {
    id: "cc-dt-5",
    title: "A customer's full name",
    topic: "data-types",
    difficulty: "beginner",
    scenario: "An account registration form stores the customer's full name, which may contain multiple words and spaces.",
    task: "Complete the declaration for customerName using the appropriate text type.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    /*BLANK*/ customerName = \"Ama Owusu\";\n    cout << customerName;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["string"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string customerName = \"Ama Owusu\";\n    cout << customerName;\n    return 0;\n}",
    testCases: ["Output: Ama Owusu"],
    explanation: "string (from the <string> header) stores sequences of characters of any length, including spaces — exactly what a full name needs. char can only hold a single character, so it can't represent a multi-word name at all.",
    commonMistake: "Trying to use char for a full name, which can only ever hold one character — 'Ama Owusu' would not fit, and the compiler would reject it.",
    hints: ["A full name is a sequence of many characters, not just one.", "The multi-character text type in C++ is string."],
    tags: ["string", "data-types", "text"],
  },
  {
    id: "cc-dt-6",
    title: "Integer division surprise",
    topic: "data-types",
    difficulty: "intermediate",
    scenario: "A teacher splits a total score of 9 marks evenly between 2 students and is surprised the program prints 4 instead of 4.5.",
    task: "Complete the declaration of average using the correct type so the fractional part isn't lost.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int total = 9;\n    /*BLANK*/ average = total / 2.0;\n    cout << average;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["double"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int total = 9;\n    double average = total / 2.0;\n    cout << average;\n    return 0;\n}",
    testCases: ["total = 9 -> average = 4.5"],
    explanation: "Even though total is an int, dividing by 2.0 (a double literal) forces the whole division to happen in floating point, producing 4.5 — but that result must be stored in a double variable, or it would be truncated back down to 4 when assigned to an int. Both the division and the storage type matter here.",
    commonMistake: "Declaring average as int even though the division itself is done in floating point — the correct floating-point result (4.5) gets silently truncated to 4 the moment it's assigned to an int variable.",
    hints: ["The division 9 / 2.0 correctly produces 4.5 — but what type must hold that fractional result?", "average needs to be a double, not an int, to actually keep the .5."],
    tags: ["double", "integer-division", "type-selection"],
  },
  {
    id: "cc-dt-7",
    title: "A transaction reference number",
    topic: "data-types",
    difficulty: "beginner",
    scenario: "Each transaction is assigned a whole-number reference ID, like 100234.",
    task: "Complete the type declaration for transactionId.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    /*BLANK*/ transactionId = 100234;\n    cout << transactionId;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["int"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int transactionId = 100234;\n    cout << transactionId;\n    return 0;\n}",
    testCases: ["Output: 100234"],
    explanation: "A reference number like this is a whole number with no fractional part and no leading-zero formatting requirement, so int is the natural type — even though it's used as an identifier rather than for arithmetic.",
    commonMistake: "Using a string for a numeric ID just because it 'looks like a code' — this is sometimes the right call (e.g. if it has leading zeros that must be preserved), but for a plain whole number like this, int is simpler and supports numeric comparisons directly.",
    hints: ["This value is a whole number, not text and not fractional.", "int transactionId = 100234;"],
    tags: ["int", "data-types"],
  },
  {
    id: "cc-dt-8",
    title: "Account type as text",
    topic: "data-types",
    difficulty: "beginner",
    scenario: "A bank stores each account's type as a short label such as 'Savings' or 'Current'.",
    task: "Complete the declaration for accountType.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    /*BLANK*/ accountType = \"Savings\";\n    cout << accountType;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["string"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string accountType = \"Savings\";\n    cout << accountType;\n    return 0;\n}",
    testCases: ["Output: Savings"],
    explanation: "'Savings' is a sequence of characters (a word), which needs string, not char (only one character) or a numeric type. Text values that are more than a single character always need string.",
    commonMistake: "Forgetting to #include <string> — string is a library type, not a built-in keyword like int, so it must be explicitly included or the compiler won't recognise it.",
    hints: ["This is a word, made of several characters.", "string accountType = \"Savings\";"],
    tags: ["string", "data-types"],
  },
  {
    id: "cc-dt-9",
    title: "float vs double — picking the right one",
    topic: "data-types",
    difficulty: "advanced-beginner",
    scenario: "A senior developer reviewing your fintech app's code flags a variable declared as float for storing a running account balance, recommending a more precise type for financial totals.",
    task: "Complete the corrected declaration using the higher-precision floating-point type.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    /*BLANK*/ accountBalance = 15234.68;\n    cout << accountBalance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["double"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double accountBalance = 15234.68;\n    cout << accountBalance;\n    return 0;\n}",
    testCases: ["Output: 15234.68"],
    explanation: "double offers roughly double the precision of float, which matters for financial values that accumulate over many transactions — small rounding errors in float can compound into visibly wrong totals over time. This is exactly why the reviewer flagged it: it compiles fine either way, but double is the safer default for money.",
    commonMistake: "Assuming float is 'good enough' because the number looks small and simple — precision problems with float usually only become visible after many repeated calculations, not in a single isolated example.",
    hints: ["float and double are both decimal types, but they differ in precision.", "The more precise one, and the standard default for financial values, is double."],
    tags: ["double", "float", "precision", "fintech"],
  },
  {
    id: "cc-dt-10",
    title: "A transaction status character code",
    topic: "data-types",
    difficulty: "intermediate",
    scenario: "A payment system encodes each transaction's status as a single character: 'P' for pending, 'S' for success, 'F' for failed.",
    task: "Complete the declaration so status correctly stores the single-character code 'P'.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    char status = /*BLANK*/;\n    cout << status;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["'P'"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    char status = 'P';\n    cout << status;\n    return 0;\n}",
    testCases: ["Output: P"],
    explanation: "A char value is always written in single quotes, containing exactly one character. Using double quotes (\"P\") instead would make this a string literal, which cannot be assigned directly to a char variable — the types simply don't match.",
    commonMistake: "Writing \"P\" with double quotes, producing a compile error like 'cannot convert const char* to char'.",
    hints: ["char literals use one specific kind of quote mark, not the other.", "Single quotes: 'P'"],
    tags: ["char", "data-types"],
  },

  // ================= D. Input & Output (8) =================
  {
    id: "cc-io-1",
    title: "Reading a PIN from the user",
    topic: "io",
    difficulty: "beginner",
    scenario: "An ATM simulator needs to read a 4-digit PIN typed by the user.",
    task: "Complete the statement that reads a value into pin from the keyboard.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int pin;\n    /*BLANK*/;\n    cout << \"You entered: \" << pin;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["cin >> pin"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int pin;\n    cin >> pin;\n    cout << \"You entered: \" << pin;\n    return 0;\n}",
    testCases: ["User types 4521 -> program prints: You entered: 4521"],
    explanation: "cin >> pin reads a value typed by the user (up to the next whitespace) and stores it into pin. The >> operator here points 'into' the variable — the mirror image of cout << pin, which sends the value 'out' to the screen.",
    commonMistake: "Confusing the direction of the operator and writing 'pin >> cin' or 'cin << pin' — the arrows always point from the source toward the destination: cin >> pin reads INTO pin.",
    hints: ["cin is used with a specific operator to read input into a variable.", "cin >> pin;"],
    tags: ["cin", "input"],
  },
  {
    id: "cc-io-2",
    title: "Reading a name with spaces",
    topic: "io",
    difficulty: "intermediate",
    scenario: "A registration form needs to read a customer's full name, which may include a space (e.g. 'Kwame Mensah'), but plain cin stops at the first space.",
    task: "Complete the function call that correctly reads an entire line, including spaces.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string fullName;\n    /*BLANK*/(cin, fullName);\n    cout << fullName;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["getline"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string fullName;\n    getline(cin, fullName);\n    cout << fullName;\n    return 0;\n}",
    testCases: ["User types 'Kwame Mensah' -> program prints: Kwame Mensah (full name, including the space)"],
    explanation: "getline(cin, fullName) reads an entire line of input, including any spaces, up until the Enter key — unlike cin >> name, which stops at the first whitespace character and would only capture 'Kwame'.",
    commonMistake: "Using cin >> fullName for a value that might contain a space — it silently only captures the first word, with no error to warn you.",
    hints: ["Plain cin >> stops at the first space — you need a function designed to read a whole line.", "getline(cin, fullName);"],
    tags: ["getline", "input", "strings"],
  },
  {
    id: "cc-io-3",
    title: "Printing with a newline",
    topic: "io",
    difficulty: "beginner",
    scenario: "A receipt printout should show the item name, then move to a new line before printing the price.",
    task: "Complete the statement so a newline appears between the two pieces of output.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"Coffee\" << /*BLANK*/;\n    cout << 12.5;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["endl", "\"\\n\""] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    cout << \"Coffee\" << endl;\n    cout << 12.5;\n    return 0;\n}",
    testCases: ["Output:\nCoffee\n12.5"],
    explanation: "endl inserts a newline character and flushes the output buffer, moving subsequent output to a new line. The escape sequence \"\\n\" also inserts a newline (without the explicit flush) and is a valid alternative here.",
    commonMistake: "Forgetting that endl needs to be sent to cout with << just like any other value — writing bare 'endl;' on its own does nothing.",
    hints: ["This is a special value you send to cout to move to a new line.", "It's endl (or the escape sequence \"\\n\")."],
    tags: ["cout", "endl", "output"],
  },
  {
    id: "cc-io-4",
    title: "Reading an account number",
    topic: "io",
    difficulty: "beginner",
    scenario: "A teller system reads an account number typed by a customer at a self-service kiosk.",
    task: "Complete the declaration and read statement together — first declare accountNumber, then read it.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    long /*BLANK1*/;\n    cout << \"Enter account number: \";\n    /*BLANK2*/;\n    cout << accountNumber;\n    return 0;\n}",
    blanks: [
      { id: "/*BLANK1*/", kind: "inline", acceptableAnswers: ["accountNumber"] },
      { id: "/*BLANK2*/", kind: "inline", acceptableAnswers: ["cin >> accountNumber"] },
    ],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    long accountNumber;\n    cout << \"Enter account number: \";\n    cin >> accountNumber;\n    cout << accountNumber;\n    return 0;\n}",
    testCases: ["User types 1234567890 -> program prints: 1234567890"],
    explanation: "First the variable is declared (with a suitable type, long, since account numbers can be larger than a typical int comfortably holds), then cin >> reads a value from the keyboard into it. Declaring before reading is required — you can't read into a variable that doesn't exist yet.",
    commonMistake: "Trying to read into a variable in the same statement it's declared without the correct syntax, or attempting cin >> before the variable has been declared at all.",
    hints: ["First give the variable a name, then use cin to fill it.", "long accountNumber; then cin >> accountNumber;"],
    tags: ["cin", "declaration", "input"],
  },
  {
    id: "cc-io-5",
    title: "Prompting before reading",
    topic: "io",
    difficulty: "beginner",
    scenario: "A withdrawal screen should clearly ask the user to enter an amount before waiting for their input.",
    task: "Complete the output statement that prompts the user, placed before the cin call.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount;\n    /*BLANK*/;\n    cin >> amount;\n    cout << \"You requested: \" << amount;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["cout << \"Enter withdrawal amount: \""] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount;\n    cout << \"Enter withdrawal amount: \";\n    cin >> amount;\n    cout << \"You requested: \" << amount;\n    return 0;\n}",
    testCases: ["Program prints the prompt, then reads a number, e.g. user enters 100 -> prints: You requested: 100"],
    explanation: "Printing a prompt with cout before calling cin gives the user context for what they're expected to type — without it, the program just sits there waiting with no explanation, which is confusing for anyone running it.",
    commonMistake: "Placing the prompt after cin >> amount instead of before it — the prompt would then only appear after the user has already (blindly) entered a value.",
    hints: ["The prompt needs to print before cin reads anything.", "cout << \"Enter withdrawal amount: \";"],
    tags: ["cout", "cin", "prompts"],
  },
  {
    id: "cc-io-6",
    title: "Reading two values in one line",
    topic: "io",
    difficulty: "intermediate",
    scenario: "A merchant checkout screen asks the cashier to type both the quantity and unit price on one line, separated by a space.",
    task: "Complete the single statement that reads both quantity and price from one line of input.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int quantity;\n    double price;\n    cin /*BLANK*/;\n    cout << quantity * price;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: [">> quantity >> price"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int quantity;\n    double price;\n    cin >> quantity >> price;\n    cout << quantity * price;\n    return 0;\n}",
    testCases: ["Input: 3 12.5 -> Output: 37.5"],
    explanation: "The >> operator can be chained just like <<, so cin >> quantity >> price reads two whitespace-separated values from the same input stream in one statement, filling quantity first and then price.",
    commonMistake: "Writing two separate cin statements when one chained statement would do — this still works correctly, but chaining is the idiomatic C++ way to read several values from one line.",
    hints: ["Just like cout can chain << multiple times, cin can chain >> multiple times.", "cin >> quantity >> price;"],
    tags: ["cin", "chaining", "input"],
  },
  {
    id: "cc-io-7",
    title: "Formatting output with a label",
    topic: "io",
    difficulty: "beginner",
    scenario: "A payslip printout should clearly label the net pay value, not just print a bare number.",
    task: "Complete the output statement to include a text label before the value.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double netPay = 3200.50;\n    cout << /*BLANK*/ << netPay;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["\"Net Pay: \""] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double netPay = 3200.50;\n    cout << \"Net Pay: \" << netPay;\n    return 0;\n}",
    testCases: ["Output: Net Pay: 3200.5"],
    explanation: "String literals (text in double quotes) can be chained with << just like variables — this is the standard way to build a labelled, human-readable line of output combining fixed text and a computed value.",
    commonMistake: "Forgetting the double quotes around the label text, which would make the compiler try to interpret Net and Pay as undeclared variable names instead of literal text.",
    hints: ["You need a string literal (in double quotes) chained before the variable.", "cout << \"Net Pay: \" << netPay;"],
    tags: ["cout", "output", "formatting"],
  },
  {
    id: "cc-io-8",
    title: "Reading student information",
    topic: "io",
    difficulty: "beginner",
    scenario: "A university registration console asks a student to type their ID number.",
    task: "Complete the statement so studentId is read from the keyboard.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int studentId;\n    cout << \"Enter student ID: \";\n    /*BLANK*/;\n    cout << \"Registered ID: \" << studentId;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["cin >> studentId"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int studentId;\n    cout << \"Enter student ID: \";\n    cin >> studentId;\n    cout << \"Registered ID: \" << studentId;\n    return 0;\n}",
    testCases: ["User enters 20345 -> prints: Registered ID: 20345"],
    explanation: "cin >> studentId reads a whole number typed by the user directly into the studentId variable — the same read pattern used for any numeric input, whether it's a PIN, an account number, or a student ID.",
    commonMistake: "Mixing up which variable comes after >>, or forgetting the semicolon at the end of the statement.",
    hints: ["This is the same pattern as reading a PIN — read into the variable using cin.", "cin >> studentId;"],
    tags: ["cin", "input"],
  },

  // ================= E. Arithmetic Operators (10) =================
  {
    id: "cc-ar-1",
    title: "Deducting a withdrawal",
    topic: "arithmetic",
    difficulty: "beginner",
    scenario: "An ATM needs to reduce the account balance by the amount a customer withdraws.",
    task: "Complete the compound assignment that subtracts withdrawal from balance.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 500;\n    double withdrawal = 150;\n    /*BLANK*/\n    cout << balance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["balance -= withdrawal;", "balance -= withdrawal", "balance = balance - withdrawal;", "balance = balance - withdrawal"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 500;\n    double withdrawal = 150;\n    balance -= withdrawal;\n    cout << balance;\n    return 0;\n}",
    testCases: ["balance=500, withdrawal=150 -> balance becomes 350"],
    explanation: "balance -= withdrawal; is shorthand for balance = balance - withdrawal; — it subtracts withdrawal from balance and stores the result back into balance in one compact step. Compound assignment operators like this are extremely common for updating running totals.",
    commonMistake: "Writing balance = withdrawal; by accident, which overwrites the balance entirely instead of subtracting from it — losing track of which side the assignment reduces.",
    hints: ["This combines subtraction with assignment in one operator.", "It's -=, the compound subtraction-assignment operator."],
    tags: ["compound-assignment", "subtraction", "fintech"],
  },
  {
    id: "cc-ar-2",
    title: "Adding a deposit",
    topic: "arithmetic",
    difficulty: "beginner",
    scenario: "A mobile wallet needs to increase the balance by the amount the user deposits.",
    task: "Complete the compound assignment that adds deposit to balance.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 300;\n    double deposit = 120;\n    /*BLANK*/\n    cout << balance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["balance += deposit;", "balance += deposit", "balance = balance + deposit;", "balance = balance + deposit"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 300;\n    double deposit = 120;\n    balance += deposit;\n    cout << balance;\n    return 0;\n}",
    testCases: ["balance=300, deposit=120 -> balance becomes 420"],
    explanation: "balance += deposit; adds deposit to balance and saves the result back into balance, all in one operator — shorthand for balance = balance + deposit;.",
    commonMistake: "Using -= by mistake when the operation is actually an addition (a deposit increases the balance, a withdrawal decreases it) — easy to mix up under exam pressure.",
    hints: ["This combines addition with assignment.", "It's +=."],
    tags: ["compound-assignment", "addition", "fintech"],
  },
  {
    id: "cc-ar-3",
    title: "True average, not integer division",
    topic: "arithmetic",
    difficulty: "intermediate",
    scenario: "A teacher needs the exact average of a total score of 9 split between 2 students, including the decimal part.",
    task: "Complete the division so the fractional part isn't lost.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int total = 9;\n    double average = total /*BLANK*/;\n    cout << average;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["/ 2.0", "/2.0"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int total = 9;\n    double average = total / 2.0;\n    cout << average;\n    return 0;\n}",
    testCases: ["total=9 -> average = 4.5 (not 4)"],
    explanation: "When both operands of / are integers, C++ performs integer division and truncates any remainder — 9 / 2 would give 4, discarding the .5. Dividing by 2.0 (a double literal) instead forces the whole expression into floating-point arithmetic, correctly producing 4.5.",
    commonMistake: "Writing total / 2 (integer division) and being surprised the result is 4 instead of 4.5 — the fractional part isn't rounded, it's simply thrown away.",
    hints: ["Dividing by a whole number 2 keeps the division entirely in integers.", "Divide by 2.0 instead, to force floating-point division."],
    tags: ["integer-division", "floating-point", "traps"],
  },
  {
    id: "cc-ar-4",
    title: "Calculating a transaction fee",
    topic: "arithmetic",
    difficulty: "beginner",
    scenario: "A payment platform charges a flat 2% transaction fee on every payment.",
    task: "Complete the expression that calculates the fee as 2% of amount.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount = 1000;\n    double fee = amount /*BLANK*/;\n    cout << fee;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["* 0.02", "*0.02"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount = 1000;\n    double fee = amount * 0.02;\n    cout << fee;\n    return 0;\n}",
    testCases: ["amount=1000 -> fee = 20"],
    explanation: "2% as a decimal fraction is 0.02, so multiplying amount by 0.02 calculates 2% of it directly. This is the standard pattern for calculating any percentage-based fee, tax, or discount.",
    commonMistake: "Multiplying by 2 instead of 0.02 — forgetting to convert the percentage into its decimal form (dividing by 100) before using it in the calculation.",
    hints: ["2% as a decimal is 2/100.", "amount * 0.02"],
    tags: ["percentage", "multiplication", "fintech"],
  },
  {
    id: "cc-ar-5",
    title: "The remainder operator for parity",
    topic: "arithmetic",
    difficulty: "intermediate",
    scenario: "A ticket booking system alternates seat colours based on whether the seat number is even or odd.",
    task: "Complete the expression that computes the remainder of seatNumber divided by 2.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int seatNumber = 17;\n    int remainder = /*BLANK*/;\n    cout << remainder;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["seatNumber % 2"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int seatNumber = 17;\n    int remainder = seatNumber % 2;\n    cout << remainder;\n    return 0;\n}",
    testCases: ["seatNumber=17 -> remainder = 1 (odd); seatNumber=18 -> remainder = 0 (even)"],
    explanation: "% is the modulo operator — it returns the remainder left over after integer division. Since any number's remainder after dividing by 2 is either 0 (even) or 1 (odd), % 2 is the standard way to test parity.",
    commonMistake: "Confusing % (modulo/remainder) with / (division) — they answer different questions: 17 / 2 is 8 (how many times 2 fits), while 17 % 2 is 1 (what's left over).",
    hints: ["You need the remainder after division, not the quotient.", "The modulo operator is %."],
    tags: ["modulo", "remainder", "operators"],
  },
  {
    id: "cc-ar-6",
    title: "Doubling a savings goal, step by step",
    topic: "arithmetic",
    difficulty: "beginner",
    scenario: "A savings app increases a customer's monthly deposit target using the compound multiplication operator.",
    task: "Complete the compound assignment that doubles target.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double target = 250;\n    /*BLANK*/\n    cout << target;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["target *= 2;", "target *= 2", "target = target * 2;", "target = target * 2"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double target = 250;\n    target *= 2;\n    cout << target;\n    return 0;\n}",
    testCases: ["target=250 -> becomes 500"],
    explanation: "target *= 2; multiplies target by 2 and stores the result back into target — shorthand for target = target * 2;.",
    commonMistake: "Writing target = 2; by accident (overwriting instead of multiplying) — always double-check whether you meant a compound operator or a plain reassignment.",
    hints: ["This combines multiplication with assignment.", "It's *=."],
    tags: ["compound-assignment", "multiplication"],
  },
  {
    id: "cc-ar-7",
    title: "Splitting a restaurant bill",
    topic: "arithmetic",
    difficulty: "beginner",
    scenario: "A group of friends wants to split a restaurant bill of 180 evenly among 4 people.",
    task: "Complete the division expression to calculate each person's share.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double bill = 180;\n    int people = 4;\n    double share = /*BLANK*/;\n    cout << share;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["bill / people"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double bill = 180;\n    int people = 4;\n    double share = bill / people;\n    cout << share;\n    return 0;\n}",
    testCases: ["bill=180, people=4 -> share = 45"],
    explanation: "Since bill is a double, dividing by people (even though it's an int) automatically produces a floating-point result here — the division operator promotes the whole expression to double when at least one operand already is one.",
    commonMistake: "Assuming this needs a special conversion just because people is an int — because bill is already a double, the division is floating-point automatically, with no extra .0 needed.",
    hints: ["You want the total split evenly among a count of people.", "bill / people"],
    tags: ["division", "arithmetic"],
  },
  {
    id: "cc-ar-8",
    title: "Incrementing a loyalty points counter",
    topic: "arithmetic",
    difficulty: "beginner",
    scenario: "A shop's loyalty program adds exactly 1 point every time a customer completes a purchase.",
    task: "Complete the statement that increases points by exactly 1.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int points = 14;\n    /*BLANK*/;\n    cout << points;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["points++", "++points", "points += 1"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int points = 14;\n    points++;\n    cout << points;\n    return 0;\n}",
    testCases: ["points=14 -> becomes 15"],
    explanation: "points++ (post-increment) adds exactly 1 to points. As a standalone statement like this, points++ and ++points (pre-increment) produce the identical end result — they only differ when the increment's own value is used immediately within a larger expression.",
    commonMistake: "Writing points + 1; on its own — this computes a new value but throws it away without storing it anywhere, so points never actually changes.",
    hints: ["There's a shorthand operator for 'add exactly 1'.", "points++;"],
    tags: ["increment", "operators"],
  },
  {
    id: "cc-ar-9",
    title: "Total price for multiple items",
    topic: "arithmetic",
    difficulty: "beginner",
    scenario: "A merchant checkout needs to calculate the total price for a quantity of the same item.",
    task: "Complete the expression that multiplies quantity by unitPrice.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int quantity = 3;\n    double unitPrice = 12.5;\n    double total = /*BLANK*/;\n    cout << total;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["quantity * unitPrice"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int quantity = 3;\n    double unitPrice = 12.5;\n    double total = quantity * unitPrice;\n    cout << total;\n    return 0;\n}",
    testCases: ["quantity=3, unitPrice=12.5 -> total = 37.5"],
    explanation: "Multiplication (*) computes the total cost by scaling the unit price by how many units are being purchased — the standard pattern behind every 'quantity times price' calculation.",
    commonMistake: "Using + instead of * — adding quantity and unitPrice together produces a meaningless number that isn't a total cost at all.",
    hints: ["Total cost is quantity times price, not quantity plus price.", "quantity * unitPrice"],
    tags: ["multiplication", "arithmetic"],
  },
  {
    id: "cc-ar-10",
    title: "Applying a discount",
    topic: "arithmetic",
    difficulty: "advanced-beginner",
    scenario: "An online store applies a 15% discount to a product's original price at checkout.",
    task: "Complete the expression that calculates the discounted price in a single line.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double price = 200;\n    double finalPrice = price - (price /*BLANK*/);\n    cout << finalPrice;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["* 0.15", "*0.15"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double price = 200;\n    double finalPrice = price - (price * 0.15);\n    cout << finalPrice;\n    return 0;\n}",
    testCases: ["price=200 -> discount = 30, finalPrice = 170"],
    explanation: "price * 0.15 calculates 15% of the price (the discount amount), and subtracting that from the original price gives the final, discounted price — combining a percentage calculation with a subtraction in one expression.",
    commonMistake: "Subtracting 0.15 directly from price (price - 0.15) instead of subtracting 15% OF price — this treats the discount as a fixed 0.15 currency units rather than a percentage.",
    hints: ["First figure out what 15% of the price actually is, in currency, then subtract that amount.", "price - (price * 0.15)"],
    tags: ["percentage", "discount", "arithmetic"],
  },

  // ================= F. Comparison Operators (8) =================
  {
    id: "cc-cmp-1",
    title: "Age verification",
    topic: "comparison",
    difficulty: "beginner",
    scenario: "A financial platform's login system only allows account creation for users who are at least 18 years old.",
    task: "Complete the missing condition so users below 18 are denied access.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int age = 20;\n    if (/*BLANK*/) {\n        cout << \"Access granted\";\n    } else {\n        cout << \"Access denied\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["age >= 18"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int age = 20;\n    if (age >= 18) {\n        cout << \"Access granted\";\n    } else {\n        cout << \"Access denied\";\n    }\n    return 0;\n}",
    testCases: ["17 -> Access denied", "18 -> Access granted", "25 -> Access granted"],
    explanation: ">= means 'greater than or equal to', so an age of exactly 18 correctly passes the condition. Using > instead would incorrectly reject someone who is exactly 18, since strictly-greater-than excludes the boundary value itself.",
    commonMistake: "Using age > 18 instead of age >= 18, which excludes 18-year-olds even though the requirement is 'at least 18'.",
    hints: ["Think about the minimum age allowed — should 18 itself pass?", "Which comparison operator means 'greater than or equal to'?"],
    tags: ["comparison", "age-verification", "fintech"],
  },
  {
    id: "cc-cmp-2",
    title: "Sufficient balance check",
    topic: "comparison",
    difficulty: "beginner",
    scenario: "An ATM must only allow a withdrawal if the account balance is enough to cover it.",
    task: "Complete the condition that checks whether balance can cover the withdrawal amount.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 500;\n    double amount = 200;\n    if (/*BLANK*/) {\n        cout << \"Approved\";\n    } else {\n        cout << \"Declined\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["balance >= amount"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 500;\n    double amount = 200;\n    if (balance >= amount) {\n        cout << \"Approved\";\n    } else {\n        cout << \"Declined\";\n    }\n    return 0;\n}",
    testCases: ["balance=500, amount=200 -> Approved", "balance=100, amount=200 -> Declined", "balance=200, amount=200 -> Approved (exactly enough)"],
    explanation: ">= correctly allows a withdrawal that exactly equals the balance (leaving 0), which is a valid transaction. Using strict > would incorrectly decline a withdrawal that empties the account exactly.",
    commonMistake: "Using balance > amount, which wrongly declines a withdrawal that would leave the balance at exactly zero — a perfectly valid transaction.",
    hints: ["Should a withdrawal that exactly empties the account be allowed?", "balance >= amount"],
    tags: ["comparison", "atm", "fintech"],
  },
  {
    id: "cc-cmp-3",
    title: "Checking a wrong PIN",
    topic: "comparison",
    difficulty: "beginner",
    scenario: "An ATM system needs to detect when the entered PIN does not match the stored PIN.",
    task: "Complete the condition using the 'not equal to' operator.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int enteredPin = 4321;\n    int correctPin = 1234;\n    if (/*BLANK*/) {\n        cout << \"Incorrect PIN\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["enteredPin != correctPin"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int enteredPin = 4321;\n    int correctPin = 1234;\n    if (enteredPin != correctPin) {\n        cout << \"Incorrect PIN\";\n    }\n    return 0;\n}",
    testCases: ["entered=4321, correct=1234 -> Incorrect PIN printed", "entered=1234, correct=1234 -> nothing printed"],
    explanation: "!= means 'not equal to' — it evaluates to true exactly when the two values differ, which is exactly the condition for flagging a wrong PIN entry.",
    commonMistake: "Using a single = (assignment) instead of != or == (comparison) — writing 'if (enteredPin = correctPin)' would assign correctPin's value into enteredPin and always evaluate as true (or based on that value), not compare them at all.",
    hints: ["You need the operator meaning 'is different from'.", "It's !=."],
    tags: ["comparison", "not-equal", "authentication"],
  },
  {
    id: "cc-cmp-4",
    title: "Exact PIN match",
    topic: "comparison",
    difficulty: "beginner",
    scenario: "A login screen grants access only when the entered PIN exactly matches the stored PIN.",
    task: "Complete the condition using the equality operator (not the assignment operator).",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int enteredPin = 1234;\n    int correctPin = 1234;\n    if (/*BLANK*/) {\n        cout << \"Access granted\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["enteredPin == correctPin"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int enteredPin = 1234;\n    int correctPin = 1234;\n    if (enteredPin == correctPin) {\n        cout << \"Access granted\";\n    }\n    return 0;\n}",
    testCases: ["entered=1234, correct=1234 -> Access granted"],
    explanation: "== is the equality comparison operator — two equal signs. A single = is assignment (it changes a variable's value), while == checks whether two values are the same without changing anything, which is exactly what a PIN check needs.",
    commonMistake: "Using a single = instead of == — this is one of the most common beginner C++ bugs, since if (enteredPin = correctPin) compiles without error but assigns instead of compares.",
    hints: ["Comparison for equality uses two of the same symbol, not one.", "It's ==, not =."],
    tags: ["comparison", "equality", "authentication"],
  },
  {
    id: "cc-cmp-5",
    title: "Minimum balance requirement",
    topic: "comparison",
    difficulty: "beginner",
    scenario: "A savings account requires a minimum balance of 50 to remain open without a penalty fee.",
    task: "Complete the condition that checks if balance has fallen below the minimum.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 35;\n    double minimumBalance = 50;\n    if (/*BLANK*/) {\n        cout << \"Penalty fee applies\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["balance < minimumBalance"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 35;\n    double minimumBalance = 50;\n    if (balance < minimumBalance) {\n        cout << \"Penalty fee applies\";\n    }\n    return 0;\n}",
    testCases: ["balance=35 -> Penalty fee applies", "balance=50 -> nothing printed (meets minimum exactly)"],
    explanation: "< (strictly less than) triggers the penalty only when the balance falls below the minimum — a balance exactly at 50 meets the requirement and should not be penalised, so a strict < (not <=) is correct here.",
    commonMistake: "Using <= instead of <, which would incorrectly apply the penalty even when the balance exactly equals the minimum requirement.",
    hints: ["Should a balance exactly equal to the minimum trigger a penalty?", "balance < minimumBalance"],
    tags: ["comparison", "banking", "fintech"],
  },
  {
    id: "cc-cmp-6",
    title: "Transaction limit check",
    topic: "comparison",
    difficulty: "beginner",
    scenario: "A mobile money service caps single transactions at 5000; larger amounts must be declined.",
    task: "Complete the condition that checks whether amount exceeds the transaction limit.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount = 6000;\n    double limit = 5000;\n    if (/*BLANK*/) {\n        cout << \"Transaction declined: over limit\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["amount > limit"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount = 6000;\n    double limit = 5000;\n    if (amount > limit) {\n        cout << \"Transaction declined: over limit\";\n    }\n    return 0;\n}",
    testCases: ["amount=6000, limit=5000 -> declined", "amount=5000, limit=5000 -> not declined (exactly at the limit is allowed)"],
    explanation: "> (strictly greater than) declines only amounts that exceed the limit — an amount exactly equal to the limit should still be allowed, so strict > (not >=) correctly permits the boundary value.",
    commonMistake: "Using >= instead of >, which would incorrectly decline a transaction that is exactly at the allowed limit.",
    hints: ["Should a transaction of exactly 5000 be declined?", "amount > limit"],
    tags: ["comparison", "transaction-limit", "fintech"],
  },
  {
    id: "cc-cmp-7",
    title: "Loan eligibility by credit score",
    topic: "comparison",
    difficulty: "beginner",
    scenario: "A bank only considers loan applicants whose credit score is at least 650.",
    task: "Complete the condition using the correct comparison operator.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int creditScore = 650;\n    if (/*BLANK*/) {\n        cout << \"Eligible for review\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["creditScore >= 650"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int creditScore = 650;\n    if (creditScore >= 650) {\n        cout << \"Eligible for review\";\n    }\n    return 0;\n}",
    testCases: ["creditScore=650 -> Eligible for review", "creditScore=649 -> nothing printed"],
    explanation: "'At least 650' includes the value 650 itself, so >= is required. With a credit score of exactly 650, the condition must evaluate to true — exactly what >= (but not a strict >) guarantees.",
    commonMistake: "Using > 650 instead of >= 650, which would exclude an applicant with a score of exactly 650 even though the requirement says 'at least 650'.",
    hints: ["'At least' means the boundary value itself should count.", "creditScore >= 650"],
    tags: ["comparison", "loans", "fintech"],
  },
  {
    id: "cc-cmp-8",
    title: "Maximum PIN attempts reached",
    topic: "comparison",
    difficulty: "beginner",
    scenario: "An ATM locks the card after exactly 3 failed PIN attempts.",
    task: "Complete the condition that checks whether attempts has reached the maximum of 3.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int attempts = 3;\n    if (/*BLANK*/) {\n        cout << \"Card locked\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["attempts >= 3"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int attempts = 3;\n    if (attempts >= 3) {\n        cout << \"Card locked\";\n    }\n    return 0;\n}",
    testCases: ["attempts=3 -> Card locked", "attempts=2 -> nothing printed"],
    explanation: "Using >= (rather than ==) is safer here: it locks the card the moment attempts reaches 3 or more, guarding against the count ever accidentally exceeding 3 without triggering the lock (which == 3 alone would miss if attempts somehow skipped past 3).",
    commonMistake: "Using == 3 instead of >= 3 — this happens to work if attempts is always incremented by exactly 1 each time, but is fragile if the count could ever jump past 3 without stopping exactly on it.",
    hints: ["Which operator is safe even if attempts could somehow exceed exactly 3?", "attempts >= 3"],
    tags: ["comparison", "security", "fintech"],
  },

  // ================= G. Logical Operators (8) =================
  {
    id: "cc-log-1",
    title: "Loan eligibility: two conditions must hold",
    topic: "logical",
    difficulty: "beginner",
    scenario: "A bank approves a loan only when the applicant's income is at least 5000 AND their credit score is at least 650 — both conditions are required.",
    task: "Complete the condition combining both requirements.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double income = 6000;\n    int creditScore = 700;\n    if (/*BLANK*/) {\n        cout << \"Loan approved\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["income >= 5000 && creditScore >= 650"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double income = 6000;\n    int creditScore = 700;\n    if (income >= 5000 && creditScore >= 650) {\n        cout << \"Loan approved\";\n    }\n    return 0;\n}",
    testCases: ["income=6000, credit=700 -> approved", "income=4000, credit=700 -> not approved (income too low)"],
    explanation: "&& (logical AND) requires BOTH sides to be true for the whole condition to be true — exactly matching a requirement stated with the word 'and'. If either the income or the credit score falls short, the loan is not approved.",
    commonMistake: "Using || (OR) instead of && (AND) — this would approve the loan if EITHER condition is met, which is far too lenient for a requirement that both must hold.",
    hints: ["Both conditions must be true — which operator requires that?", "It's &&, the logical AND operator."],
    tags: ["logical-and", "loans", "fintech"],
  },
  {
    id: "cc-log-2",
    title: "ATM access: PIN correct AND account active",
    topic: "logical",
    difficulty: "beginner",
    scenario: "An ATM only grants access when the PIN is correct AND the account is currently active (not suspended).",
    task: "Complete the condition combining both boolean flags.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool correctPin = true;\n    bool accountActive = true;\n    if (/*BLANK*/) {\n        cout << \"Access granted\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["correctPin && accountActive"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool correctPin = true;\n    bool accountActive = true;\n    if (correctPin && accountActive) {\n        cout << \"Access granted\";\n    }\n    return 0;\n}",
    testCases: ["correctPin=true, accountActive=true -> granted", "correctPin=true, accountActive=false -> not granted"],
    explanation: "A bool variable can be used directly in a condition (no need to write == true). && requires both correctPin and accountActive to be true, correctly denying access if the account has been suspended even with the right PIN.",
    commonMistake: "Writing correctPin == true && accountActive == true — this works but is redundant; a bool already represents true/false directly, so 'if (correctPin && accountActive)' is the cleaner, idiomatic form.",
    hints: ["Both the PIN check and the account-active check must hold.", "correctPin && accountActive"],
    tags: ["logical-and", "authentication", "fintech"],
  },
  {
    id: "cc-log-3",
    title: "Payment allowed: enough balance OR overdraft enabled",
    topic: "logical",
    difficulty: "beginner",
    scenario: "A payment can go through if the balance covers it, OR if the account has overdraft protection enabled.",
    task: "Complete the condition combining these two alternatives.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 50;\n    double amount = 100;\n    bool overdraftEnabled = true;\n    if (/*BLANK*/) {\n        cout << \"Payment allowed\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["balance >= amount || overdraftEnabled"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 50;\n    double amount = 100;\n    bool overdraftEnabled = true;\n    if (balance >= amount || overdraftEnabled) {\n        cout << \"Payment allowed\";\n    }\n    return 0;\n}",
    testCases: ["balance=50, amount=100, overdraft=true -> allowed", "balance=50, amount=100, overdraft=false -> not allowed"],
    explanation: "|| (logical OR) only needs ONE side to be true — matching a requirement stated with 'or'. Here, even though the balance alone isn't enough, overdraft protection being enabled is sufficient on its own to allow the payment.",
    commonMistake: "Using && (AND) instead of || (OR) — this would wrongly require BOTH sufficient balance AND overdraft enabled, when only one or the other should be needed.",
    hints: ["Only one of the two conditions needs to be true here.", "It's ||, the logical OR operator."],
    tags: ["logical-or", "payments", "fintech"],
  },
  {
    id: "cc-log-4",
    title: "Negating an account-locked flag",
    topic: "logical",
    difficulty: "beginner",
    scenario: "A login system should only proceed with the PIN check when the account is NOT locked.",
    task: "Complete the condition using the logical NOT operator on isLocked.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool isLocked = false;\n    if (/*BLANK*/) {\n        cout << \"Proceed to PIN entry\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["!isLocked"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool isLocked = false;\n    if (!isLocked) {\n        cout << \"Proceed to PIN entry\";\n    }\n    return 0;\n}",
    testCases: ["isLocked=false -> Proceed to PIN entry", "isLocked=true -> nothing printed"],
    explanation: "! (logical NOT) flips a boolean's value — !isLocked is true exactly when isLocked is false. This reads naturally as 'if not locked', matching the requirement directly.",
    commonMistake: "Writing isLocked == false instead of !isLocked — both work, but ! is the more idiomatic, concise way to express negation of a bool in C++.",
    hints: ["You need to flip a true/false value to its opposite.", "The NOT operator is !, placed directly before the variable."],
    tags: ["logical-not", "authentication"],
  },
  {
    id: "cc-log-5",
    title: "Account approval: adult AND valid ID",
    topic: "logical",
    difficulty: "intermediate",
    scenario: "A bank approves a new account only if the applicant is at least 18 years old AND has a valid government ID on file.",
    task: "Complete the condition combining the age check and the ID-valid flag.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int age = 22;\n    bool hasValidId = true;\n    if (/*BLANK*/) {\n        cout << \"Account approved\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["age >= 18 && hasValidId"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int age = 22;\n    bool hasValidId = true;\n    if (age >= 18 && hasValidId) {\n        cout << \"Account approved\";\n    }\n    return 0;\n}",
    testCases: ["age=22, hasValidId=true -> approved", "age=22, hasValidId=false -> not approved"],
    explanation: "Both requirements ('at least 18' AND 'has valid ID') must hold simultaneously, which is exactly what && enforces — an applicant with a valid ID but who is underage is still correctly rejected.",
    commonMistake: "Using || instead of && — this would approve an account for someone under 18 as long as they have a valid ID, which contradicts the stated requirement that both conditions must hold.",
    hints: ["Both the age and the ID requirement need to be satisfied together.", "age >= 18 && hasValidId"],
    tags: ["logical-and", "account-approval", "fintech"],
  },
  {
    id: "cc-log-6",
    title: "Free shipping: big order OR loyalty member",
    topic: "logical",
    difficulty: "intermediate",
    scenario: "An online shop offers free shipping when the order total is at least 100, OR the customer is a loyalty member (regardless of order size).",
    task: "Complete the condition combining these two alternative qualifying conditions.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double orderTotal = 40;\n    bool isLoyaltyMember = true;\n    if (/*BLANK*/) {\n        cout << \"Free shipping\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["orderTotal >= 100 || isLoyaltyMember"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double orderTotal = 40;\n    bool isLoyaltyMember = true;\n    if (orderTotal >= 100 || isLoyaltyMember) {\n        cout << \"Free shipping\";\n    }\n    return 0;\n}",
    testCases: ["orderTotal=40, isLoyaltyMember=true -> Free shipping", "orderTotal=40, isLoyaltyMember=false -> no free shipping"],
    explanation: "|| allows the customer to qualify through either path — a large enough order, or loyalty membership — without needing both at once. Here the order is small, but loyalty membership alone is enough.",
    commonMistake: "Using && instead of ||, which would incorrectly deny free shipping to a loyalty member with a small order, contradicting the 'OR' requirement.",
    hints: ["Either condition on its own should qualify the customer.", "orderTotal >= 100 || isLoyaltyMember"],
    tags: ["logical-or", "e-commerce"],
  },
  {
    id: "cc-log-7",
    title: "Transaction flagged: large amount AND new recipient",
    topic: "logical",
    difficulty: "advanced-beginner",
    scenario: "A fraud-detection system flags a mobile money transfer as suspicious only when the amount is large (over 10000) AND the recipient has never been paid before.",
    task: "Complete the condition combining both risk factors.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount = 15000;\n    bool isNewRecipient = true;\n    if (/*BLANK*/) {\n        cout << \"Transaction flagged for review\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["amount > 10000 && isNewRecipient"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount = 15000;\n    bool isNewRecipient = true;\n    if (amount > 10000 && isNewRecipient) {\n        cout << \"Transaction flagged for review\";\n    }\n    return 0;\n}",
    testCases: ["amount=15000, isNewRecipient=true -> flagged", "amount=15000, isNewRecipient=false -> not flagged (recipient already trusted)"],
    explanation: "Both risk signals must be present together to trigger a flag — a large payment to a familiar, established recipient is not automatically suspicious, so && correctly requires both conditions rather than either alone.",
    commonMistake: "Using || instead of && — this would flag every large transaction regardless of recipient history, generating far too many false alarms for legitimate, repeat payments.",
    hints: ["Both the large-amount signal and the new-recipient signal need to be true together.", "amount > 10000 && isNewRecipient"],
    tags: ["logical-and", "fraud-detection", "fintech"],
  },
  {
    id: "cc-log-8",
    title: "Combining three conditions for a discount",
    topic: "logical",
    difficulty: "advanced-beginner",
    scenario: "A shop applies a special discount when the customer is a student, AND their cart total is at least 50, AND it is not already a sale day.",
    task: "Complete the condition combining all three requirements.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool isStudent = true;\n    double cartTotal = 60;\n    bool isSaleDay = false;\n    if (/*BLANK*/) {\n        cout << \"Student discount applied\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["isStudent && cartTotal >= 50 && !isSaleDay"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool isStudent = true;\n    double cartTotal = 60;\n    bool isSaleDay = false;\n    if (isStudent && cartTotal >= 50 && !isSaleDay) {\n        cout << \"Student discount applied\";\n    }\n    return 0;\n}",
    testCases: ["isStudent=true, cartTotal=60, isSaleDay=false -> discount applied", "isStudent=true, cartTotal=60, isSaleDay=true -> not applied"],
    explanation: "All three conditions are joined with 'and', so && is used twice, combined with ! to negate isSaleDay for the 'not already a sale day' requirement. && can be chained as many times as needed — every operand must be true for the whole expression to be true.",
    commonMistake: "Mixing in || somewhere for one of the three conditions, which would let the discount apply even when one of the required conditions fails — changing the meaning from 'all must hold' to 'some must hold'.",
    hints: ["All three conditions need 'and' logic to combine.", "isStudent && cartTotal >= 50 && !isSaleDay"],
    tags: ["logical-and", "logical-not", "e-commerce"],
  },

  // ================= H. if / else (13) =================
  {
    id: "cc-ie-1",
    title: "Simple withdrawal approval",
    topic: "if-else",
    difficulty: "beginner",
    scenario: "An ATM should print 'Approved' when balance covers the withdrawal, and nothing otherwise.",
    task: "Complete the if statement (no else needed).",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 500, amount = 200;\n    /*BLANK*/ (balance >= amount) {\n        cout << \"Approved\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["if"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 500, amount = 200;\n    if (balance >= amount) {\n        cout << \"Approved\";\n    }\n    return 0;\n}",
    testCases: ["balance=500, amount=200 -> Approved", "balance=100, amount=200 -> nothing printed"],
    explanation: "The if keyword introduces a conditional block that only runs when its parenthesized condition is true. With no else, nothing happens at all when the condition is false — the program simply moves on.",
    commonMistake: "Forgetting that without an else branch, a false condition means the block is silently skipped, not an error.",
    hints: ["This is the keyword that starts every conditional statement in C++.", "if (balance >= amount) { ... }"],
    tags: ["if", "conditionals"],
  },
  {
    id: "cc-ie-2",
    title: "Approved or declined",
    topic: "if-else",
    difficulty: "beginner",
    scenario: "A loan application should print one message if approved, and a different message otherwise.",
    task: "Complete the else branch.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int creditScore = 500;\n    if (creditScore >= 650) {\n        cout << \"Approved\";\n    } /*BLANK*/ {\n        cout << \"Declined\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["else"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int creditScore = 500;\n    if (creditScore >= 650) {\n        cout << \"Approved\";\n    } else {\n        cout << \"Declined\";\n    }\n    return 0;\n}",
    testCases: ["creditScore=500 -> Declined", "creditScore=700 -> Approved"],
    explanation: "else attaches to the immediately preceding if and runs exactly when that if's condition was false — the two branches are mutually exclusive, so exactly one of them always executes.",
    commonMistake: "Writing a second, independent if instead of else — this happens to give the same result here, but is less efficient and can behave differently once more conditions are added.",
    hints: ["What keyword pairs with if to handle the 'otherwise' case?", "else"],
    tags: ["else", "conditionals"],
  },
  {
    id: "cc-ie-3",
    title: "Three-way grading with else if",
    topic: "if-else",
    difficulty: "intermediate",
    scenario: "A grading system assigns 'Pass' for scores 50+, 'Merit' for 75+, and 'Distinction' for 90+, checked from highest to lowest.",
    task: "Complete the else-if chain (the missing middle branch).",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int score = 80;\n    if (score >= 90) {\n        cout << \"Distinction\";\n    } /*BLANK*/ (score >= 75) {\n        cout << \"Merit\";\n    } else if (score >= 50) {\n        cout << \"Pass\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["else if"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int score = 80;\n    if (score >= 90) {\n        cout << \"Distinction\";\n    } else if (score >= 75) {\n        cout << \"Merit\";\n    } else if (score >= 50) {\n        cout << \"Pass\";\n    }\n    return 0;\n}",
    testCases: ["score=80 -> Merit", "score=95 -> Distinction", "score=60 -> Pass"],
    explanation: "else if chains conditions so only the first matching branch runs — checking from highest threshold down means a score of 80 correctly stops at 'Merit' rather than falling through to check 'Pass' too, since the chain short-circuits once a branch matches.",
    commonMistake: "Writing separate independent if statements instead of an else-if chain — with a score of 80, standalone ifs for >=75 and >=50 would both fire, printing 'MeritPass' instead of just 'Merit'.",
    hints: ["This connects a new condition to the previous if-else without starting a fresh, independent check.", "else if (score >= 75)"],
    tags: ["else-if", "conditionals", "chaining"],
  },
  {
    id: "cc-ie-4",
    title: "Nested check: PIN then balance",
    topic: "if-else",
    difficulty: "intermediate",
    scenario: "An ATM first checks the PIN; only if that's correct does it then check whether the balance is sufficient.",
    task: "Complete the nested if condition checking balance, placed inside the outer PIN check.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool correctPin = true;\n    double balance = 300, amount = 500;\n    if (correctPin) {\n        if (/*BLANK*/) {\n            cout << \"Dispensing cash\";\n        } else {\n            cout << \"Insufficient funds\";\n        }\n    } else {\n        cout << \"Wrong PIN\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["balance >= amount"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool correctPin = true;\n    double balance = 300, amount = 500;\n    if (correctPin) {\n        if (balance >= amount) {\n            cout << \"Dispensing cash\";\n        } else {\n            cout << \"Insufficient funds\";\n        }\n    } else {\n        cout << \"Wrong PIN\";\n    }\n    return 0;\n}",
    testCases: ["correctPin=true, balance=300, amount=500 -> Insufficient funds", "correctPin=false -> Wrong PIN (balance never checked)"],
    explanation: "Nested if statements let you check a second condition only after a first one has already passed — here, the balance is only ever examined once the PIN has already been confirmed correct, matching a real ATM's actual sequence of checks.",
    commonMistake: "Flattening this into a single && condition (correctPin && balance >= amount) — that would work for this exact case but loses the ability to give a distinct 'Wrong PIN' message versus an 'Insufficient funds' message.",
    hints: ["This inner condition compares the balance against the requested amount.", "balance >= amount"],
    tags: ["nested-if", "conditionals", "atm"],
  },
  {
    id: "cc-ie-5",
    title: "Even or odd checker",
    topic: "if-else",
    difficulty: "beginner",
    scenario: "A seat-numbering system needs to label each seat as 'Even row' or 'Odd row'.",
    task: "Complete the condition checking whether seatNumber is even.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int seatNumber = 14;\n    if (/*BLANK*/) {\n        cout << \"Even row\";\n    } else {\n        cout << \"Odd row\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["seatNumber % 2 == 0"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int seatNumber = 14;\n    if (seatNumber % 2 == 0) {\n        cout << \"Even row\";\n    } else {\n        cout << \"Odd row\";\n    }\n    return 0;\n}",
    testCases: ["seatNumber=14 -> Even row", "seatNumber=7 -> Odd row"],
    explanation: "% 2 gives the remainder after dividing by 2, which is 0 for every even number — combining % with == 0 is the standard C++ idiom for testing evenness.",
    commonMistake: "Writing seatNumber % 2 without comparing it to anything — the expression itself evaluates to 0 or 1, which happens to work in an if-condition (0 is 'false', nonzero is 'true'), but backwards from the intended meaning (it would treat odd numbers as 'true'/even-branch).",
    hints: ["You need both the modulo operator and a comparison to zero.", "seatNumber % 2 == 0"],
    tags: ["modulo", "conditionals", "if-else"],
  },
  {
    id: "cc-ie-6",
    title: "Overdraft warning threshold",
    topic: "if-else",
    difficulty: "intermediate",
    scenario: "A banking app shows a red warning when balance drops below 0, a yellow warning below 100, and no warning otherwise.",
    task: "Complete the middle else-if branch for the yellow warning.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 50;\n    if (balance < 0) {\n        cout << \"Red warning\";\n    } else if (/*BLANK*/) {\n        cout << \"Yellow warning\";\n    } else {\n        cout << \"No warning\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["balance < 100"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 50;\n    if (balance < 0) {\n        cout << \"Red warning\";\n    } else if (balance < 100) {\n        cout << \"Yellow warning\";\n    } else {\n        cout << \"No warning\";\n    }\n    return 0;\n}",
    testCases: ["balance=50 -> Yellow warning", "balance=-10 -> Red warning", "balance=200 -> No warning"],
    explanation: "Because this branch is only reached when the first condition (balance < 0) was already false, the else-if only needs to check balance < 100 — it doesn't need to also re-check balance >= 0, since that's already implied by having reached this point.",
    commonMistake: "Over-specifying the condition as 'balance >= 0 && balance < 100' — this is correct but unnecessary; the else-if chain already guarantees the earlier condition failed.",
    hints: ["This branch is only reached once balance < 0 has already failed, so you only need to check the upper bound.", "balance < 100"],
    tags: ["else-if", "conditionals", "fintech"],
  },
  {
    id: "cc-ie-7",
    title: "Ternary operator for a quick label",
    topic: "if-else",
    difficulty: "advanced-beginner",
    scenario: "A checkout screen needs a one-line label: 'Member' if isMember is true, otherwise 'Guest'.",
    task: "Complete the ternary conditional expression.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool isMember = false;\n    string label = isMember /*BLANK*/ \"Member\" : \"Guest\";\n    cout << label;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["?"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool isMember = false;\n    string label = isMember ? \"Member\" : \"Guest\";\n    cout << label;\n    return 0;\n}",
    testCases: ["isMember=false -> Guest", "isMember=true -> Member"],
    explanation: "The ternary operator condition ? valueIfTrue : valueIfFalse packs a simple if-else into a single expression that produces a value directly — useful for short, one-line choices like this label, though it's best kept simple since nesting ternaries hurts readability.",
    commonMistake: "Forgetting that the ternary is an expression, not a statement — trying to put a full cout statement or multiple statements inside either branch doesn't work the way a regular if-else block would.",
    hints: ["The ternary operator uses two punctuation marks: one after the condition, one between the two results.", "isMember ? \"Member\" : \"Guest\""],
    tags: ["ternary", "conditionals"],
  },
  {
    id: "cc-ie-8",
    title: "Weather-based advice",
    topic: "if-else",
    difficulty: "beginner",
    scenario: "A weather app tells the user to carry an umbrella if it's raining.",
    task: "Complete the condition checking the isRaining flag.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool isRaining = true;\n    if (/*BLANK*/) {\n        cout << \"Carry an umbrella\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["isRaining"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool isRaining = true;\n    if (isRaining) {\n        cout << \"Carry an umbrella\";\n    }\n    return 0;\n}",
    testCases: ["isRaining=true -> Carry an umbrella", "isRaining=false -> nothing printed"],
    explanation: "A bool variable can be used directly as an if condition — no need to compare it to true explicitly, since if (isRaining) already means exactly 'if isRaining is true'.",
    commonMistake: "Writing if (isRaining == true) — this works but is redundant; the cleaner, idiomatic form is just if (isRaining).",
    hints: ["A bool can be placed directly inside the if-condition parentheses.", "if (isRaining)"],
    tags: ["bool", "conditionals", "if"],
  },
  {
    id: "cc-ie-9",
    title: "Ticket pricing by age bracket",
    topic: "if-else",
    difficulty: "intermediate",
    scenario: "A cinema charges different ticket prices: children (under 12) pay 5, seniors (65+) pay 6, everyone else pays 10.",
    task: "Complete the final else branch for the general admission price.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int age = 30;\n    double price;\n    if (age < 12) {\n        price = 5;\n    } else if (age >= 65) {\n        price = 6;\n    } /*BLANK*/ {\n        price = 10;\n    }\n    cout << price;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["else"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int age = 30;\n    double price;\n    if (age < 12) {\n        price = 5;\n    } else if (age >= 65) {\n        price = 6;\n    } else {\n        price = 10;\n    }\n    cout << price;\n    return 0;\n}",
    testCases: ["age=30 -> 10", "age=8 -> 5", "age=70 -> 6"],
    explanation: "A final, unconditional else catches every case not already matched by the earlier if/else-if branches — here, anyone who is neither under 12 nor 65+ falls into general admission.",
    commonMistake: "Writing 'else if (age >= 12 && age < 65)' instead of a plain else — this is logically equivalent but unnecessarily re-checks conditions already guaranteed by having reached this branch.",
    hints: ["This is the catch-all branch for everyone not matched above — no condition needed.", "else"],
    tags: ["else", "conditionals", "pricing"],
  },
  {
    id: "cc-ie-10",
    title: "Curly braces for a multi-statement branch",
    topic: "if-else",
    difficulty: "intermediate",
    scenario: "A student's code only prints the warning message when overdrawn, but a second line meant to run in the same branch executes unconditionally due to missing braces.",
    task: "Complete the opening brace so both statements belong to the if block.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 100;\n    if (balance < 0) /*BLANK*/\n        cout << \"Overdrawn!\";\n        cout << \" Contact support.\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["{"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 100;\n    if (balance < 0) {\n        cout << \"Overdrawn!\";\n        cout << \" Contact support.\";\n    }\n    return 0;\n}",
    testCases: ["balance=100 -> nothing printed (both lines correctly skipped)", "balance=-20 -> Overdrawn! Contact support."],
    explanation: "Without braces, an if only controls the single statement immediately following it — every subsequent line runs unconditionally regardless of the if's condition. Wrapping both statements in { } makes them a single block that the if correctly controls together.",
    commonMistake: "Assuming indentation alone groups statements together in C++ — unlike Python, indentation is purely cosmetic; only { } actually defines a block's boundaries.",
    hints: ["Without braces, if only applies to the one statement right after it — you need to group two statements together.", "Add an opening brace { right after the condition."],
    tags: ["braces", "conditionals", "common-bug"],
  },
  {
    id: "cc-ie-11",
    title: "Currency conversion eligibility",
    topic: "if-else",
    difficulty: "beginner",
    scenario: "A currency exchange kiosk only processes conversions for amounts greater than zero.",
    task: "Complete the else branch that handles an invalid (non-positive) amount.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount = -50;\n    if (amount > 0) {\n        cout << \"Processing conversion\";\n    } else {\n        cout << /*BLANK*/;\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["\"Invalid amount\""] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount = -50;\n    if (amount > 0) {\n        cout << \"Processing conversion\";\n    } else {\n        cout << \"Invalid amount\";\n    }\n    return 0;\n}",
    testCases: ["amount=-50 -> Invalid amount", "amount=50 -> Processing conversion"],
    explanation: "The else branch runs precisely when amount > 0 is false — that is, when amount is zero or negative — making it the right place to report the input as invalid.",
    commonMistake: "Forgetting the double quotes around the string literal, which would make the compiler try to interpret Invalid and amount as undeclared identifiers.",
    hints: ["You need a string literal in double quotes.", "\"Invalid amount\""],
    tags: ["else", "validation", "fintech"],
  },
  {
    id: "cc-ie-12",
    title: "Library book return status",
    topic: "if-else",
    difficulty: "intermediate",
    scenario: "A library system prints 'On time' if daysLate is 0, 'Late fee applies' if daysLate is positive, and flags an error if daysLate is somehow negative.",
    task: "Complete the middle condition for a positive daysLate.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int daysLate = 3;\n    if (daysLate < 0) {\n        cout << \"Invalid data\";\n    } else if (/*BLANK*/) {\n        cout << \"Late fee applies\";\n    } else {\n        cout << \"On time\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["daysLate > 0"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int daysLate = 3;\n    if (daysLate < 0) {\n        cout << \"Invalid data\";\n    } else if (daysLate > 0) {\n        cout << \"Late fee applies\";\n    } else {\n        cout << \"On time\";\n    }\n    return 0;\n}",
    testCases: ["daysLate=3 -> Late fee applies", "daysLate=0 -> On time", "daysLate=-1 -> Invalid data"],
    explanation: "Since the first branch already ruled out negative values, this else-if only needs to check for strictly positive — the final else then correctly and exclusively captures the remaining case of exactly 0.",
    commonMistake: "Using >= 0 here instead of > 0, which would incorrectly claim a late fee applies even when daysLate is exactly 0 (on time).",
    hints: ["This branch should only match strictly positive values — 0 belongs in the final else.", "daysLate > 0"],
    tags: ["else-if", "conditionals"],
  },
  {
    id: "cc-ie-13",
    title: "Nested condition with a compound check",
    topic: "if-else",
    difficulty: "advanced-beginner",
    scenario: "A merchant POS system approves a card payment only if the card is not expired, and additionally requires a signature only when the amount exceeds 200.",
    task: "Complete the nested if condition that checks whether a signature is required.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool cardExpired = false;\n    double amount = 350;\n    if (!cardExpired) {\n        if (/*BLANK*/) {\n            cout << \"Signature required\";\n        } else {\n            cout << \"Approved, no signature needed\";\n        }\n    } else {\n        cout << \"Card declined: expired\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["amount > 200"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    bool cardExpired = false;\n    double amount = 350;\n    if (!cardExpired) {\n        if (amount > 200) {\n            cout << \"Signature required\";\n        } else {\n            cout << \"Approved, no signature needed\";\n        }\n    } else {\n        cout << \"Card declined: expired\";\n    }\n    return 0;\n}",
    testCases: ["cardExpired=false, amount=350 -> Signature required", "cardExpired=true -> Card declined: expired (amount never checked)"],
    explanation: "Nesting the amount check inside the not-expired branch ensures the signature question is only even considered for a valid card — an expired card is declined immediately without ever evaluating the amount.",
    commonMistake: "Combining both checks into one flat condition with && — that loses the ability to give the specific 'Card declined: expired' message versus the signature-related messages.",
    hints: ["This inner condition compares the payment amount to the signature threshold.", "amount > 200"],
    tags: ["nested-if", "conditionals", "fintech"],
  },

  // ================= I. switch (6) =================
  {
    id: "cc-sw-1",
    title: "Menu selection with switch",
    topic: "switch",
    difficulty: "intermediate",
    scenario: "An ATM's main menu handles a numeric choice: 1 for Withdraw, 2 for Deposit, 3 for Balance.",
    task: "Complete the switch statement's keyword.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int choice = 2;\n    /*BLANK*/ (choice) {\n        case 1: cout << \"Withdraw\"; break;\n        case 2: cout << \"Deposit\"; break;\n        case 3: cout << \"Balance\"; break;\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["switch"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int choice = 2;\n    switch (choice) {\n        case 1: cout << \"Withdraw\"; break;\n        case 2: cout << \"Deposit\"; break;\n        case 3: cout << \"Balance\"; break;\n    }\n    return 0;\n}",
    testCases: ["choice=2 -> Deposit"],
    explanation: "switch evaluates one expression once, then jumps straight to the matching case label — a cleaner alternative to a long else-if chain when comparing one variable against several exact constant values.",
    commonMistake: "Trying to use switch with a condition like a range (case < 5) — switch only supports matching exact constant values, not ranges or general boolean expressions.",
    hints: ["This keyword starts a multi-way branch based on one value.", "switch (choice) { ... }"],
    tags: ["switch", "conditionals"],
  },
  {
    id: "cc-sw-2",
    title: "Missing break causes fall-through",
    topic: "switch",
    difficulty: "intermediate",
    scenario: "A day-of-week labeller prints the wrong output because a case is missing its break, causing execution to fall through into the next case.",
    task: "Complete the missing break statement for case 1.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int day = 1;\n    switch (day) {\n        case 1: cout << \"Monday\"; /*BLANK*/\n        case 2: cout << \"Tuesday\"; break;\n        default: cout << \"Other\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["break;"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int day = 1;\n    switch (day) {\n        case 1: cout << \"Monday\"; break;\n        case 2: cout << \"Tuesday\"; break;\n        default: cout << \"Other\";\n    }\n    return 0;\n}",
    testCases: ["day=1 -> Monday (only)"],
    explanation: "Without break, execution 'falls through' into the next case's code regardless of whether its label matches — here, omitting break after case 1 would print both 'Monday' and 'Tuesday'. break exits the switch immediately after the matched case's code runs.",
    commonMistake: "Assuming switch automatically stops after one matching case like if-else does — it doesn't; each case needs its own explicit break unless fall-through is intentional.",
    hints: ["Without this statement, execution continues into the next case below.", "break;"],
    tags: ["switch", "break", "fall-through"],
  },
  {
    id: "cc-sw-3",
    title: "Default case for unrecognized input",
    topic: "switch",
    difficulty: "beginner",
    scenario: "A vending machine's switch statement should handle any button code that doesn't match a known snack.",
    task: "Complete the label that catches any unmatched value.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int code = 9;\n    switch (code) {\n        case 1: cout << \"Chips\"; break;\n        case 2: cout << \"Soda\"; break;\n        /*BLANK*/: cout << \"Invalid selection\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["default"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int code = 9;\n    switch (code) {\n        case 1: cout << \"Chips\"; break;\n        case 2: cout << \"Soda\"; break;\n        default: cout << \"Invalid selection\";\n    }\n    return 0;\n}",
    testCases: ["code=9 -> Invalid selection", "code=1 -> Chips"],
    explanation: "default acts like the else at the end of an if/else-if chain — it runs when none of the case labels match the switch expression's value, making it the natural place to handle unexpected input.",
    commonMistake: "Forgetting default entirely — without it, an unmatched value simply falls through the whole switch doing nothing, with no feedback to the user at all.",
    hints: ["This is the catch-all label for any value not matched by an earlier case.", "default:"],
    tags: ["switch", "default"],
  },
  {
    id: "cc-sw-4",
    title: "Grade letter from a character",
    topic: "switch",
    difficulty: "intermediate",
    scenario: "A grading report converts a letter grade character into a descriptive word using switch.",
    task: "Complete the case label matching the character 'B'.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    char grade = 'B';\n    switch (grade) {\n        case 'A': cout << \"Excellent\"; break;\n        /*BLANK*/ cout << \"Good\"; break;\n        case 'C': cout << \"Average\"; break;\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["case 'B':"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    char grade = 'B';\n    switch (grade) {\n        case 'A': cout << \"Excellent\"; break;\n        case 'B': cout << \"Good\"; break;\n        case 'C': cout << \"Average\"; break;\n    }\n    return 0;\n}",
    testCases: ["grade='B' -> Good"],
    explanation: "switch works with char values just as well as int values — each case label must be a constant, and 'B' (single-quoted char literal) matches the char variable grade directly.",
    commonMistake: "Writing case \"B\": with double quotes — that's a string literal, not a char, and switch on a char variable requires char literals (single quotes) as its case labels.",
    hints: ["Match the char value with a case label using single quotes.", "case 'B':"],
    tags: ["switch", "char"],
  },
  {
    id: "cc-sw-5",
    title: "Combining multiple cases with shared behavior",
    topic: "switch",
    difficulty: "advanced-beginner",
    scenario: "A calendar app treats Saturday and Sunday both as 'Weekend' by letting one case fall through into the next.",
    task: "Complete the first case label so it deliberately falls through to share the Sunday case's code.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int day = 6;\n    switch (day) {\n        /*BLANK*/\n        case 7: cout << \"Weekend\"; break;\n        default: cout << \"Weekday\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["case 6:"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int day = 6;\n    switch (day) {\n        case 6:\n        case 7: cout << \"Weekend\"; break;\n        default: cout << \"Weekday\";\n    }\n    return 0;\n}",
    testCases: ["day=6 -> Weekend", "day=7 -> Weekend", "day=3 -> Weekday"],
    explanation: "Stacking case 6: directly above case 7: with no break in between is intentional fall-through — day 6 has no code of its own, so control simply continues into case 7's code, letting both values share one output.",
    commonMistake: "Adding a break after case 6: by mistake — that would defeat the intentional sharing and make day 6 print nothing at all.",
    hints: ["This case label has no code of its own — it should fall straight through into the next case.", "case 6:"],
    tags: ["switch", "fall-through", "intentional"],
  },
  {
    id: "cc-sw-6",
    title: "Payment method routing",
    topic: "switch",
    difficulty: "intermediate",
    scenario: "A checkout system routes to a different processor function name based on a payment method code: 1=Card, 2=MobileMoney, 3=Cash.",
    task: "Complete the switch expression itself, using the paymentMethod variable.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int paymentMethod = 2;\n    switch (/*BLANK*/) {\n        case 1: cout << \"Processing card\"; break;\n        case 2: cout << \"Processing mobile money\"; break;\n        case 3: cout << \"Processing cash\"; break;\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["paymentMethod"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int paymentMethod = 2;\n    switch (paymentMethod) {\n        case 1: cout << \"Processing card\"; break;\n        case 2: cout << \"Processing mobile money\"; break;\n        case 3: cout << \"Processing cash\"; break;\n    }\n    return 0;\n}",
    testCases: ["paymentMethod=2 -> Processing mobile money"],
    explanation: "The expression inside switch(...) is evaluated exactly once, and its value is compared against each case label in turn — here that expression is simply the variable paymentMethod itself.",
    commonMistake: "Putting a full condition like paymentMethod == 2 inside the switch parentheses — switch takes a plain value to compare, not a boolean expression; the comparisons happen implicitly against each case label.",
    hints: ["switch needs the variable whose value it will compare against each case.", "paymentMethod"],
    tags: ["switch", "fintech"],
  },

  // ================= J. Loops (13) =================
  {
    id: "cc-lp-1",
    title: "Printing receipts for a fixed count",
    topic: "loops",
    difficulty: "beginner",
    scenario: "A print queue needs to print 'Receipt' exactly 5 times.",
    task: "Complete the for loop's condition so it runs exactly 5 times.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    for (int i = 0; /*BLANK*/; i++) {\n        cout << \"Receipt \";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["i < 5"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    for (int i = 0; i < 5; i++) {\n        cout << \"Receipt \";\n    }\n    return 0;\n}",
    testCases: ["Prints 'Receipt ' five times total"],
    explanation: "A for loop's middle part is its continuation condition, checked before every iteration — with i starting at 0 and incrementing by 1 each time, i < 5 allows exactly the iterations i=0,1,2,3,4, which is 5 total.",
    commonMistake: "Using i <= 5 instead of i < 5 — with i starting at 0, that would run 6 times (0 through 5 inclusive), one too many.",
    hints: ["i starts at 0 — what upper bound gives exactly 5 iterations?", "i < 5"],
    tags: ["for-loop", "loops"],
  },
  {
    id: "cc-lp-2",
    title: "Summing deposits with a while loop",
    topic: "loops",
    difficulty: "intermediate",
    scenario: "A savings tracker keeps adding a fixed monthly deposit to a total until the total reaches at least 1000.",
    task: "Complete the while loop's condition.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double total = 0, monthly = 150;\n    while (/*BLANK*/) {\n        total += monthly;\n    }\n    cout << total;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["total < 1000"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double total = 0, monthly = 150;\n    while (total < 1000) {\n        total += monthly;\n    }\n    cout << total;\n    return 0;\n}",
    testCases: ["monthly=150 -> loop runs 7 times, total = 1050"],
    explanation: "A while loop checks its condition before every iteration and keeps running as long as it stays true — total < 1000 correctly keeps adding deposits until the total first reaches or exceeds 1000, at which point the loop stops.",
    commonMistake: "Using total <= 1000, which would let one more unwanted deposit through if a deposit landed exactly on 1000 in some other scenario — < 1000 is the precise 'stop once we've reached the goal' condition.",
    hints: ["The loop should keep running while the goal has not yet been reached.", "total < 1000"],
    tags: ["while-loop", "loops", "fintech"],
  },
  {
    id: "cc-lp-3",
    title: "Guaranteed at-least-once PIN prompt",
    topic: "loops",
    difficulty: "intermediate",
    scenario: "An ATM should prompt for a PIN at least once, and keep re-prompting (in this simplified simulation) while the entered PIN is wrong.",
    task: "Complete the do-while loop's trailing keyword.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int enteredPin = 1234, correctPin = 1234, attempts = 0;\n    do {\n        attempts++;\n    } /*BLANK*/ (enteredPin != correctPin && attempts < 3);\n    cout << attempts;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["while"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int enteredPin = 1234, correctPin = 1234, attempts = 0;\n    do {\n        attempts++;\n    } while (enteredPin != correctPin && attempts < 3);\n    cout << attempts;\n    return 0;\n}",
    testCases: ["enteredPin equals correctPin -> loop body runs exactly once, attempts = 1"],
    explanation: "A do-while loop checks its condition AFTER the body runs, guaranteeing the body executes at least once — this matters for prompts, where you always want to ask at least one time before deciding whether to ask again.",
    commonMistake: "Using a plain while loop instead when 'at least once' behavior is required — a while loop checks its condition first and might skip the body entirely if the condition starts out false.",
    hints: ["This loop type checks its condition after the body, guaranteeing at least one run.", "do { ... } while (condition);"],
    tags: ["do-while", "loops", "atm"],
  },
  {
    id: "cc-lp-4",
    title: "Skipping a zero-amount transaction",
    topic: "loops",
    difficulty: "advanced-beginner",
    scenario: "A transaction processor loops through a batch and should skip (not process) any transaction with an amount of exactly 0, but continue checking the rest.",
    task: "Complete the statement that skips to the next iteration.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int amounts[3] = {50, 0, 75};\n    for (int i = 0; i < 3; i++) {\n        if (amounts[i] == 0) {\n            /*BLANK*/;\n        }\n        cout << \"Processed \" << amounts[i] << \" \";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["continue"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int amounts[3] = {50, 0, 75};\n    for (int i = 0; i < 3; i++) {\n        if (amounts[i] == 0) {\n            continue;\n        }\n        cout << \"Processed \" << amounts[i] << \" \";\n    }\n    return 0;\n}",
    testCases: ["amounts={50,0,75} -> prints: Processed 50 Processed 75  (the 0 amount is skipped)"],
    explanation: "continue immediately jumps to the next iteration of the loop, skipping any remaining code in the current iteration's body — here, it skips the cout line for a zero-amount transaction while letting the loop carry on to the next index.",
    commonMistake: "Using break instead of continue — break would stop the entire loop early, processing no further transactions at all, rather than just skipping the one bad entry.",
    hints: ["You want to skip just this one iteration, not stop the whole loop.", "continue;"],
    tags: ["continue", "loops", "fintech"],
  },
  {
    id: "cc-lp-5",
    title: "Stopping early once a match is found",
    topic: "loops",
    difficulty: "advanced-beginner",
    scenario: "A search through customer IDs should stop looping the moment a target ID is found, rather than continuing to check the rest.",
    task: "Complete the statement that exits the loop entirely once found.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int ids[4] = {101, 205, 309, 412};\n    int target = 309;\n    for (int i = 0; i < 4; i++) {\n        if (ids[i] == target) {\n            cout << \"Found at index \" << i;\n            /*BLANK*/;\n        }\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["break"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int ids[4] = {101, 205, 309, 412};\n    int target = 309;\n    for (int i = 0; i < 4; i++) {\n        if (ids[i] == target) {\n            cout << \"Found at index \" << i;\n            break;\n        }\n    }\n    return 0;\n}",
    testCases: ["target=309 -> prints: Found at index 2, then the loop stops immediately"],
    explanation: "break immediately exits the entire enclosing loop, skipping all remaining iterations — appropriate once the search has already found what it was looking for and there's no reason to keep checking.",
    commonMistake: "Using continue instead of break — continue would only skip to the next iteration, but the loop would keep running and potentially waste time checking indices after the match was already found.",
    hints: ["You want to stop the whole loop immediately, not just skip to the next iteration.", "break;"],
    tags: ["break", "loops", "search"],
  },
  {
    id: "cc-lp-6",
    title: "Range-based for loop over an array",
    topic: "loops",
    difficulty: "intermediate",
    scenario: "A payroll program needs to sum a list of employee salaries using a modern range-based for loop instead of an index.",
    task: "Complete the range-based for loop header.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int salaries[3] = {1200, 1500, 1800};\n    int total = 0;\n    for (int s /*BLANK*/ salaries) {\n        total += s;\n    }\n    cout << total;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: [":"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int salaries[3] = {1200, 1500, 1800};\n    int total = 0;\n    for (int s : salaries) {\n        total += s;\n    }\n    cout << total;\n    return 0;\n}",
    testCases: ["salaries={1200,1500,1800} -> total = 4500"],
    explanation: "A range-based for loop uses a colon to mean 'for each element in' — for (int s : salaries) reads naturally as 'for each s in salaries', automatically visiting every element without needing an explicit index variable.",
    commonMistake: "Using 'in' (like Python) instead of a colon — C++'s range-based for loop syntax specifically uses a colon, not the word 'in'.",
    hints: ["Range-based for loops separate the variable and the container with a specific punctuation mark, not a word.", "for (int s : salaries)"],
    tags: ["range-for", "loops", "arrays"],
  },
  {
    id: "cc-lp-7",
    title: "Nested loops for a multiplication table",
    topic: "loops",
    difficulty: "intermediate",
    scenario: "A small multiplication table (1-3) needs a nested loop, with the inner loop running fully for each outer iteration.",
    task: "Complete the inner loop's initialization.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    for (int i = 1; i <= 3; i++) {\n        for (int j = /*BLANK*/; j <= 3; j++) {\n            cout << i * j << \" \";\n        }\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["1"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    for (int i = 1; i <= 3; i++) {\n        for (int j = 1; j <= 3; j++) {\n            cout << i * j << \" \";\n        }\n    }\n    return 0;\n}",
    testCases: ["Prints: 1 2 3 2 4 6 3 6 9  (9 values total)"],
    explanation: "In a nested loop, the inner loop runs to completion for every single iteration of the outer loop — with both starting at 1, this produces all 9 combinations of i (1-3) times j (1-3), the classic pattern for a small multiplication table.",
    commonMistake: "Starting the inner loop at 0 by habit, which would produce an extra unwanted row/column of zeros (i*0 = 0) not part of a 1-to-3 multiplication table.",
    hints: ["The table should start from 1, not 0.", "int j = 1"],
    tags: ["nested-loops", "for-loop"],
  },
  {
    id: "cc-lp-8",
    title: "Counting down for a countdown timer",
    topic: "loops",
    difficulty: "beginner",
    scenario: "A simple countdown timer prints 5, 4, 3, 2, 1 before printing 'Go!'.",
    task: "Complete the decrement in the for loop's update step.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    for (int i = 5; i >= 1; /*BLANK*/) {\n        cout << i << \" \";\n    }\n    cout << \"Go!\";\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["i--"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    for (int i = 5; i >= 1; i--) {\n        cout << i << \" \";\n    }\n    cout << \"Go!\";\n    return 0;\n}",
    testCases: ["Prints: 5 4 3 2 1 Go!"],
    explanation: "i-- decreases i by 1 after each iteration, counting down from 5 to 1 — the loop's update step doesn't have to increase the counter; it can just as easily decrease it, as long as the condition (i >= 1) is designed to eventually become false.",
    commonMistake: "Using i++ (increment) instead of i-- (decrement) — with i starting at 5 and the condition i >= 1, incrementing would never make the condition false, causing an infinite loop.",
    hints: ["You need i to decrease each time, not increase.", "i--"],
    tags: ["for-loop", "decrement", "loops"],
  },
  {
    id: "cc-lp-9",
    title: "While loop reading until a sentinel value",
    topic: "loops",
    difficulty: "intermediate",
    scenario: "A sales-entry program reads sale amounts one at a time and stops as soon as the user enters -1 (a sentinel value meaning 'done').",
    task: "Complete the while loop's condition, checking the just-read amount against the sentinel.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount;\n    double total = 0;\n    cin >> amount;\n    while (/*BLANK*/) {\n        total += amount;\n        cin >> amount;\n    }\n    cout << total;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["amount != -1"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount;\n    double total = 0;\n    cin >> amount;\n    while (amount != -1) {\n        total += amount;\n        cin >> amount;\n    }\n    cout << total;\n    return 0;\n}",
    testCases: ["Input: 100 200 -1 -> total = 300"],
    explanation: "This is the classic 'read-before-loop, then read-at-end-of-body' sentinel pattern: the first cin happens before the loop so the while condition has a real value to check immediately, and each iteration reads the next value at its end so the condition is re-evaluated with fresh input.",
    commonMistake: "Forgetting the initial cin >> amount before the loop — without it, the while condition would check an uninitialized amount on the very first pass.",
    hints: ["The loop should keep going as long as the sentinel value hasn't been entered.", "amount != -1"],
    tags: ["while-loop", "sentinel", "loops"],
  },
  {
    id: "cc-lp-10",
    title: "Summing array elements with a for loop",
    topic: "loops",
    difficulty: "beginner",
    scenario: "A shopping cart total needs to sum every price stored in an array.",
    task: "Complete the loop body that adds each element to total.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double prices[4] = {10.5, 20, 5.25, 8};\n    double total = 0;\n    for (int i = 0; i < 4; i++) {\n        /*BLANK*/;\n    }\n    cout << total;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["total += prices[i]"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double prices[4] = {10.5, 20, 5.25, 8};\n    double total = 0;\n    for (int i = 0; i < 4; i++) {\n        total += prices[i];\n    }\n    cout << total;\n    return 0;\n}",
    testCases: ["prices={10.5,20,5.25,8} -> total = 43.75"],
    explanation: "Inside the loop, prices[i] accesses the current element at index i, and += accumulates it into the running total — this is the standard pattern for summing every element of an array using an indexed for loop.",
    commonMistake: "Writing total += prices; (forgetting the [i] index entirely) — prices alone refers to the whole array, not a single number, and would not compile as an operand of +=.",
    hints: ["You need to add the current element, accessed by index i, to the running total.", "total += prices[i];"],
    tags: ["for-loop", "arrays", "accumulation"],
  },
  {
    id: "cc-lp-11",
    title: "Infinite loop with a manual break condition",
    topic: "loops",
    difficulty: "advanced-beginner",
    scenario: "A menu-driven program loops forever showing options, exiting only when the user explicitly selects 'quit' — modeled here with a while(true) loop.",
    task: "Complete the while loop's always-true condition.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int choice = 0;\n    int attempts = 0;\n    while (/*BLANK*/) {\n        attempts++;\n        choice = 5;\n        if (choice == 5) break;\n    }\n    cout << attempts;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["true"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int choice = 0;\n    int attempts = 0;\n    while (true) {\n        attempts++;\n        choice = 5;\n        if (choice == 5) break;\n    }\n    cout << attempts;\n    return 0;\n}",
    testCases: ["Loop runs exactly once (attempts=1), then breaks when choice becomes 5"],
    explanation: "while (true) creates a loop with no built-in stopping condition — it relies entirely on an internal break statement to end. This pattern is common for menu loops where the exit condition (user picks 'quit') is more naturally checked inside the loop body than in the loop header.",
    commonMistake: "Forgetting to include a break anywhere inside a while(true) loop — this creates a genuine infinite loop that never terminates, which is a serious bug (and, on a real system, could hang the program).",
    hints: ["This loop is designed to run forever unless something inside it explicitly stops it.", "while (true)"],
    tags: ["while-true", "loops", "break"],
  },
  {
    id: "cc-lp-12",
    title: "Validating input with a retry loop",
    topic: "loops",
    difficulty: "advanced-beginner",
    scenario: "A deposit form should keep asking for an amount as long as the entered value is negative (invalid).",
    task: "Complete the do-while condition that keeps re-prompting for invalid input.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount = -5;\n    int loops = 0;\n    do {\n        loops++;\n        amount = 20;\n    } while (/*BLANK*/);\n    cout << loops;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["amount < 0"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double amount = -5;\n    int loops = 0;\n    do {\n        loops++;\n        amount = 20;\n    } while (amount < 0);\n    cout << loops;\n    return 0;\n}",
    testCases: ["Loop runs once (loops=1) since amount becomes 20, which is no longer negative"],
    explanation: "The do-while re-checks amount < 0 after each attempt — as long as the entered amount stays negative, the body (which here simulates a re-prompt) runs again; once a valid, non-negative amount is entered, the loop stops.",
    commonMistake: "Using amount <= 0 instead of amount < 0 — an amount of exactly 0 might be perfectly valid (e.g., checking a balance), so the retry condition should only target genuinely invalid negative values, matching the scenario's stated rule.",
    hints: ["The loop should keep retrying only while the amount is invalid — specifically negative.", "amount < 0"],
    tags: ["do-while", "validation", "loops"],
  },
  {
    id: "cc-lp-13",
    title: "Compounding interest year over year",
    topic: "loops",
    difficulty: "intermediate",
    scenario: "A savings calculator applies 5% annual interest to a balance, repeated for 3 years, using a loop instead of writing the calculation three times.",
    task: "Complete the loop body that applies one year's interest to balance.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 1000;\n    for (int year = 1; year <= 3; year++) {\n        /*BLANK*/;\n    }\n    cout << balance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["balance += balance * 0.05", "balance *= 1.05"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 1000;\n    for (int year = 1; year <= 3; year++) {\n        balance += balance * 0.05;\n    }\n    cout << balance;\n    return 0;\n}",
    testCases: ["balance=1000 after 3 years of 5% compounding -> approximately 1157.625"],
    explanation: "Each iteration recomputes 5% of the CURRENT balance (not the original) and adds it back in — that's exactly what compounding means, and why balance must be read again fresh on every loop pass rather than always computing 5% of the original 1000.",
    commonMistake: "Writing balance += 1000 * 0.05 (always using the original starting balance) instead of balance += balance * 0.05 — this computes simple interest, not compound interest, giving a smaller and incorrect result after the first year.",
    hints: ["Each year's interest should be based on the CURRENT balance, not the original amount.", "balance += balance * 0.05;"],
    tags: ["for-loop", "compounding", "fintech"],
  },
  {
    id: "cc-lp-14",
    title: "Counting large mobile money transactions",
    topic: "loops",
    difficulty: "intermediate",
    scenario: "A mobile money (MoMo) agent wants to know how many of today's transactions were larger than 1000, out of a list of 6.",
    task: "Complete the loop body that checks each transaction and counts the large ones.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double transactions[6] = {50, 1200, 300, 5000, 80, 2000};\n    int largeCount = 0;\n    for (int i = 0; i < 6; i++) {\n        /*BLANK*/\n    }\n    cout << largeCount;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["if (transactions[i] > 1000) largeCount++;", "if (transactions[i] > 1000) largeCount++"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double transactions[6] = {50, 1200, 300, 5000, 80, 2000};\n    int largeCount = 0;\n    for (int i = 0; i < 6; i++) {\n        if (transactions[i] > 1000) largeCount++;\n    }\n    cout << largeCount;\n    return 0;\n}",
    testCases: ["transactions={50,1200,300,5000,80,2000} -> largeCount = 3 (1200, 5000, 2000)"],
    explanation: "A single-statement if body doesn't need braces in C++, so 'if (transactions[i] > 1000) largeCount++;' is a complete, valid loop body on one line: check the current element, and increment the counter only when it qualifies.",
    commonMistake: "Comparing transactions[i] to 1000 with the wrong direction (< instead of >), which would count the small transactions instead of the large ones.",
    hints: ["You need a condition on the current element, plus an increment, in one line.", "if (transactions[i] > 1000) largeCount++;"],
    tags: ["for-loop", "counting", "mobile-money"],
  },

  // ================= K. Arrays (10) =================
  {
    id: "cc-arr-1",
    title: "Declaring a fixed-size array",
    topic: "arrays",
    difficulty: "beginner",
    scenario: "A gradebook needs to store exactly 5 exam scores.",
    task: "Complete the array declaration with the correct size.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int scores[/*BLANK*/] = {70, 85, 90, 60, 75};\n    cout << scores[0];\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["5"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int scores[5] = {70, 85, 90, 60, 75};\n    cout << scores[0];\n    return 0;\n}",
    testCases: ["Output: 70"],
    explanation: "An array's declared size must match (or exceed) the number of initializer values — here, 5 values are provided, so the array is declared to hold 5 ints.",
    commonMistake: "Declaring the array with a smaller size than the number of initializers provided (e.g. scores[3] with 5 values) — this is a compile error since the initializer list would overflow the declared size.",
    hints: ["Count the values in the initializer list.", "There are 5 values, so scores[5]."],
    tags: ["arrays", "declaration"],
  },
  {
    id: "cc-arr-2",
    title: "Accessing the first element",
    topic: "arrays",
    difficulty: "beginner",
    scenario: "A queue-numbering display needs to show the first customer's ticket number, stored at the start of an array.",
    task: "Complete the index used to access the first element.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int tickets[3] = {101, 102, 103};\n    cout << tickets[/*BLANK*/];\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["0"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int tickets[3] = {101, 102, 103};\n    cout << tickets[0];\n    return 0;\n}",
    testCases: ["Output: 101"],
    explanation: "C++ arrays are zero-indexed — the first element is always at index 0, not 1. tickets[0] is 101, tickets[1] is 102, and so on.",
    commonMistake: "Using tickets[1] expecting the first element — that actually retrieves the SECOND element (102), a classic off-by-one error for anyone coming from 1-indexed languages.",
    hints: ["Array indexing starts counting from a specific number, not from 1.", "The first element is at index 0."],
    tags: ["arrays", "indexing", "zero-indexed"],
  },
  {
    id: "cc-arr-3",
    title: "Accessing the last element",
    topic: "arrays",
    difficulty: "intermediate",
    scenario: "A leaderboard needs to display the last recorded score in a 6-element array.",
    task: "Complete the index expression for the last element, using the array's size.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int scores[6] = {10, 20, 30, 40, 50, 60};\n    cout << scores[/*BLANK*/];\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["6 - 1", "5"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int scores[6] = {10, 20, 30, 40, 50, 60};\n    cout << scores[6 - 1];\n    return 0;\n}",
    testCases: ["Output: 60"],
    explanation: "For an array of size N, valid indices run from 0 to N-1 — the last valid index is always size - 1, since indexing starts at 0. Here, with size 6, the last element is at index 5 (6 - 1).",
    commonMistake: "Using scores[6] to get the 'last' element — that's actually one past the end of a 6-element array (valid indices are 0-5), which is undefined behavior, not the last element.",
    hints: ["The last valid index is the array's size minus one.", "scores[6 - 1] which is scores[5]"],
    tags: ["arrays", "indexing", "off-by-one"],
  },
  {
    id: "cc-arr-4",
    title: "Looping to find the maximum",
    topic: "arrays",
    difficulty: "intermediate",
    scenario: "A sales report needs to find the highest single sale amount from a list of daily totals.",
    task: "Complete the comparison inside the loop that updates the running maximum.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int sales[5] = {200, 450, 150, 600, 300};\n    int maxSale = sales[0];\n    for (int i = 1; i < 5; i++) {\n        if (/*BLANK*/) {\n            maxSale = sales[i];\n        }\n    }\n    cout << maxSale;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["sales[i] > maxSale"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int sales[5] = {200, 450, 150, 600, 300};\n    int maxSale = sales[0];\n    for (int i = 1; i < 5; i++) {\n        if (sales[i] > maxSale) {\n            maxSale = sales[i];\n        }\n    }\n    cout << maxSale;\n    return 0;\n}",
    testCases: ["sales={200,450,150,600,300} -> maxSale = 600"],
    explanation: "This is the standard 'running maximum' pattern: start by assuming the first element is the largest, then compare every subsequent element against the current best, replacing it whenever a larger value is found.",
    commonMistake: "Starting maxSale at 0 instead of sales[0] — this happens to work for positive sales values, but would break entirely if all values were negative (e.g., tracking losses), since 0 would incorrectly appear to be the 'maximum'.",
    hints: ["Compare the current element against the best one found so far.", "sales[i] > maxSale"],
    tags: ["arrays", "loops", "maximum"],
  },
  {
    id: "cc-arr-5",
    title: "2D array for a seating chart",
    topic: "arrays",
    difficulty: "advanced-beginner",
    scenario: "A cinema seating chart uses a 2D array with 2 rows and 3 seats per row; you need to mark one specific seat as booked.",
    task: "Complete the 2D array access for row 1, seat 2.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int seats[2][3] = {{0,0,0},{0,0,0}};\n    seats/*BLANK*/ = 1;\n    cout << seats[1][2];\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["[1][2]"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int seats[2][3] = {{0,0,0},{0,0,0}};\n    seats[1][2] = 1;\n    cout << seats[1][2];\n    return 0;\n}",
    testCases: ["Output: 1"],
    explanation: "A 2D array uses two bracket pairs: the first selects the row, the second selects the column within that row — seats[1][2] means row index 1, seat (column) index 2, both zero-indexed.",
    commonMistake: "Writing seats[1,2] (a single bracket pair with a comma) instead of seats[1][2] — that's not valid 2D-array syntax in C++; each dimension needs its own bracket pair.",
    hints: ["A 2D array needs two separate bracket pairs, not one with a comma.", "seats[1][2]"],
    tags: ["arrays", "2d-array", "indexing"],
  },
  {
    id: "cc-arr-6",
    title: "Initializing all elements to zero",
    topic: "arrays",
    difficulty: "beginner",
    scenario: "A vote-counting array for 4 candidates should start with every candidate at 0 votes.",
    task: "Complete the initializer that sets every element to 0 using the shorthand form.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int votes[4] = /*BLANK*/;\n    cout << votes[0] << votes[3];\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["{0}"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int votes[4] = {0};\n    cout << votes[0] << votes[3];\n    return 0;\n}",
    testCases: ["Output: 00 (both votes[0] and votes[3] are 0)"],
    explanation: "When an initializer list has fewer values than the array's size, C++ fills all remaining elements with 0 automatically — {0} sets the first element to 0 explicitly, and the rest follow the same zero-fill rule.",
    commonMistake: "Assuming an uninitialized array (int votes[4];  with no initializer at all) is automatically zeroed — for a local array, it is NOT; its elements start out as garbage/indeterminate values unless explicitly initialized.",
    hints: ["A short initializer list automatically zero-fills the rest of the array.", "{0}"],
    tags: ["arrays", "initialization"],
  },
  {
    id: "cc-arr-7",
    title: "Array size out of bounds",
    topic: "arrays",
    difficulty: "advanced-beginner",
    scenario: "A student's loop to print every element of a 5-item inventory array accidentally reads one element past the end.",
    task: "Fix the loop's condition so it stays within bounds.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int stock[5] = {3, 7, 2, 9, 1};\n    for (int i = 0; i /*BLANK*/; i++) {\n        cout << stock[i] << \" \";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["< 5"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int stock[5] = {3, 7, 2, 9, 1};\n    for (int i = 0; i < 5; i++) {\n        cout << stock[i] << \" \";\n    }\n    return 0;\n}",
    testCases: ["Prints: 3 7 2 9 1  (exactly 5 values, no more)"],
    explanation: "With a 5-element array, valid indices are 0 through 4 — the loop condition i < 5 stops exactly after i=4, never attempting the out-of-bounds stock[5].",
    commonMistake: "Using i <= 5 by habit, which would attempt to read stock[5] — one element past the valid range, which is undefined behavior in C++ (no automatic bounds checking on raw arrays).",
    hints: ["The array has 5 elements — what's the correct upper-bound comparison?", "i < 5"],
    tags: ["arrays", "out-of-bounds", "loops"],
  },
  {
    id: "cc-arr-8",
    title: "Array of strings for menu items",
    topic: "arrays",
    difficulty: "intermediate",
    scenario: "A restaurant ordering kiosk stores its menu item names in an array of strings.",
    task: "Complete the declaration of an array holding 3 string values.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    /*BLANK*/ menu[3] = {\"Jollof Rice\", \"Waakye\", \"Banku\"};\n    cout << menu[1];\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["string"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string menu[3] = {\"Jollof Rice\", \"Waakye\", \"Banku\"};\n    cout << menu[1];\n    return 0;\n}",
    testCases: ["Output: Waakye"],
    explanation: "An array's element type can be any type, including string — string menu[3] declares an array of 3 string objects, each holding one menu item's full text.",
    commonMistake: "Using char menu[3] expecting it to hold three words — char can only hold a single character each, not a whole word like 'Waakye'; string is required for text elements.",
    hints: ["Each element here is a whole word (multiple characters), not a single character.", "string menu[3] = {...};"],
    tags: ["arrays", "string", "menu"],
  },
  {
    id: "cc-arr-9",
    title: "Passing an array to a function",
    topic: "arrays",
    difficulty: "advanced-beginner",
    scenario: "A function needs to receive an array of temperatures and compute their sum, without knowing its exact size in advance.",
    task: "Complete the function parameter that accepts an int array along with its size.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint sumArray(int arr/*BLANK*/, int size) {\n    int total = 0;\n    for (int i = 0; i < size; i++) total += arr[i];\n    return total;\n}\n\nint main() {\n    int temps[4] = {20, 25, 18, 22};\n    cout << sumArray(temps, 4);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["[]"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint sumArray(int arr[], int size) {\n    int total = 0;\n    for (int i = 0; i < size; i++) total += arr[i];\n    return total;\n}\n\nint main() {\n    int temps[4] = {20, 25, 18, 22};\n    cout << sumArray(temps, 4);\n    return 0;\n}",
    testCases: ["temps={20,25,18,22} -> sumArray returns 85"],
    explanation: "When an array is passed as a function parameter, it decays to a pointer to its first element — the parameter is written as int arr[] (or equivalently int* arr). Because size information is lost in this decay, it's standard practice to pass the size as a separate parameter, as this function does.",
    commonMistake: "Forgetting to pass the size separately and trying to compute the array's length inside the function with sizeof(arr) — inside the function, arr has decayed to a pointer, so sizeof(arr) gives the pointer's size, not the original array's length.",
    hints: ["Array parameters use empty square brackets in the function signature.", "int arr[]"],
    tags: ["arrays", "functions", "parameters"],
  },
  {
    id: "cc-arr-10",
    title: "Searching for a value in an array",
    topic: "arrays",
    difficulty: "intermediate",
    scenario: "A student ID lookup needs to check whether a given ID exists anywhere in a roster array.",
    task: "Complete the condition that checks each element against the target ID.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int roster[4] = {2001, 2002, 2003, 2004};\n    int target = 2003;\n    bool found = false;\n    for (int i = 0; i < 4; i++) {\n        if (/*BLANK*/) {\n            found = true;\n        }\n    }\n    cout << found;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["roster[i] == target"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int roster[4] = {2001, 2002, 2003, 2004};\n    int target = 2003;\n    bool found = false;\n    for (int i = 0; i < 4; i++) {\n        if (roster[i] == target) {\n            found = true;\n        }\n    }\n    cout << found;\n    return 0;\n}",
    testCases: ["target=2003 -> found becomes true (prints 1)", "target=9999 -> found stays false (prints 0)"],
    explanation: "This is a linear search: check every element in turn with == against the target, and record whether any match was found. Since IDs are compared for exact equality (not order), == is the right comparison operator here.",
    commonMistake: "Using = instead of == inside the if condition — a single = would assign target's value into roster[i] instead of comparing them, and also wouldn't compile as a boolean condition the way intended here since it evaluates to the assigned value.",
    hints: ["You're comparing an array element to the target using equality.", "roster[i] == target"],
    tags: ["arrays", "search", "loops"],
  },
  {
    id: "cc-arr-11",
    title: "Finding the top scorer in a class",
    topic: "arrays",
    difficulty: "intermediate",
    scenario: "A student management system needs to find the highest score in a class of 5, out of an array of exam scores.",
    task: "Complete the loop body that updates topScore whenever a higher score is found.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int scores[5] = {68, 91, 74, 88, 95};\n    int topScore = scores[0];\n    for (int i = 1; i < 5; i++) {\n        /*BLANK*/\n    }\n    cout << topScore;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["if (scores[i] > topScore) topScore = scores[i];", "if (scores[i] > topScore) topScore = scores[i]"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int scores[5] = {68, 91, 74, 88, 95};\n    int topScore = scores[0];\n    for (int i = 1; i < 5; i++) {\n        if (scores[i] > topScore) topScore = scores[i];\n    }\n    cout << topScore;\n    return 0;\n}",
    testCases: ["scores={68,91,74,88,95} -> topScore = 95"],
    explanation: "This is the classic max-finding algorithm: start by assuming the first element is the largest, then compare every later element against the current best, replacing it whenever a bigger one turns up. Starting the loop at i=1 (not 0) avoids a pointless self-comparison of scores[0] against itself.",
    commonMistake: "Starting topScore at 0 instead of scores[0] — this happens to work here since all scores are positive, but breaks the moment every score in the array is negative.",
    hints: ["Compare the current element to the best one found so far, and replace it if the current one is bigger.", "if (scores[i] > topScore) topScore = scores[i];"],
    tags: ["arrays", "algorithm", "student-management"],
  },

  // ================= L. Strings (8) =================
  {
    id: "cc-str-1",
    title: "Declaring a string variable",
    topic: "strings",
    difficulty: "beginner",
    scenario: "A customer support form needs to store a customer's typed complaint as text.",
    task: "Complete the declaration for complaintText.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    /*BLANK*/ complaintText = \"Card not dispensing cash\";\n    cout << complaintText;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["string"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string complaintText = \"Card not dispensing cash\";\n    cout << complaintText;\n    return 0;\n}",
    testCases: ["Output: Card not dispensing cash"],
    explanation: "string is the standard type for text of any length in C++, backed by the <string> header — it handles memory management automatically, unlike raw C-style char arrays.",
    commonMistake: "Forgetting #include <string>, without which the compiler doesn't recognize the string type at all.",
    hints: ["This is the standard multi-character text type.", "string complaintText = \"...\";"],
    tags: ["string", "declaration"],
  },
  {
    id: "cc-str-2",
    title: "Finding a string's length",
    topic: "strings",
    difficulty: "beginner",
    scenario: "A password field needs to check how many characters the user has typed so far.",
    task: "Complete the method call that returns the number of characters in password.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string password = \"secure123\";\n    cout << password./*BLANK*/();\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["length", "size"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string password = \"secure123\";\n    cout << password.length();\n    return 0;\n}",
    testCases: ["password=\"secure123\" -> 9"],
    explanation: "The .length() method (identical to .size()) returns the number of characters currently stored in a string — 'secure123' has 9 characters, so password.length() returns 9.",
    commonMistake: "Trying to use sizeof(password) instead — sizeof on a string object gives the size of the string object's internal structure in bytes, not the number of characters it holds, which is a very different (and usually much less useful) number.",
    hints: ["This is a method call on the string object itself, using dot notation.", "password.length()"],
    tags: ["strings", "length", "methods"],
  },
  {
    id: "cc-str-3",
    title: "Concatenating first and last name",
    topic: "strings",
    difficulty: "beginner",
    scenario: "A registration form needs to combine a first name and last name into one full name string, separated by a space.",
    task: "Complete the concatenation expression.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string first = \"Kofi\";\n    string last = \"Asante\";\n    string full = first /*BLANK1*/ \" \" /*BLANK2*/ last;\n    cout << full;\n    return 0;\n}",
    blanks: [
      { id: "/*BLANK1*/", kind: "inline", acceptableAnswers: ["+"] },
      { id: "/*BLANK2*/", kind: "inline", acceptableAnswers: ["+"] },
    ],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string first = \"Kofi\";\n    string last = \"Asante\";\n    string full = first + \" \" + last;\n    cout << full;\n    return 0;\n}",
    testCases: ["first=\"Kofi\", last=\"Asante\" -> Kofi Asante"],
    explanation: "The + operator is overloaded for string to mean concatenation — joining strings end to end. Chaining first + \" \" + last builds the full name with a space in between, evaluated left to right.",
    commonMistake: "Using << to join strings the way cout does (string full = first << \" \" << last;) — << is not defined for combining strings together into a new string; that's exactly what + is for.",
    hints: ["Strings can be joined together with the same symbol used for addition.", "first + \" \" + last"],
    tags: ["strings", "concatenation"],
  },
  {
    id: "cc-str-4",
    title: "Comparing two strings for equality",
    topic: "strings",
    difficulty: "beginner",
    scenario: "A login system checks whether the entered username matches the stored one.",
    task: "Complete the comparison condition.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string entered = \"admin\";\n    string stored = \"admin\";\n    if (entered /*BLANK*/ stored) {\n        cout << \"Match\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["=="] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string entered = \"admin\";\n    string stored = \"admin\";\n    if (entered == stored) {\n        cout << \"Match\";\n    }\n    return 0;\n}",
    testCases: ["entered=\"admin\", stored=\"admin\" -> Match"],
    explanation: "Unlike raw C-style char arrays (where you'd need strcmp), C++'s string type overloads == to compare content directly — entered == stored is true exactly when both strings contain the same characters in the same order.",
    commonMistake: "Assuming == compares memory addresses (as it effectively does for C-style char* strings) rather than content — for std::string specifically, == is overloaded to do a proper content comparison, so this concern doesn't apply here.",
    hints: ["std::string supports direct content comparison with a familiar operator.", "entered == stored"],
    tags: ["strings", "comparison", "authentication"],
  },
  {
    id: "cc-str-5",
    title: "Extracting a substring",
    topic: "strings",
    difficulty: "intermediate",
    scenario: "A card-masking feature needs to extract just the last 4 digits of a 16-digit card number string for display.",
    task: "Complete the substr call extracting the last 4 characters, starting at index 12.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string cardNumber = \"4111222233334444\";\n    string lastFour = cardNumber./*BLANK*/(13, 4);\n    cout << lastFour;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["substr"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string cardNumber = \"4111222233334444\";\n    string lastFour = cardNumber.substr(13, 4);\n    cout << lastFour;\n    return 0;\n}",
    testCases: ["cardNumber=\"4111222233334444\" (17 chars in this example) -> substr(13,4) extracts \"4444\""],
    explanation: "substr(startIndex, count) returns a new string containing count characters starting at startIndex — exactly what's needed to pull out a fixed-length suffix like the last 4 digits of a card number for masked display.",
    commonMistake: "Confusing substr's two arguments — the second argument is a COUNT of characters to take, not an end index, so substr(13, 4) means 'starting at index 13, take 4 characters', not 'from index 13 to index 4'.",
    hints: ["This method extracts a portion of a string given a start position and a length.", "cardNumber.substr(13, 4)"],
    tags: ["strings", "substr", "fintech"],
  },
  {
    id: "cc-str-6",
    title: "Finding a character within a string",
    topic: "strings",
    difficulty: "intermediate",
    scenario: "An email validation check looks for the '@' symbol somewhere inside a typed email address.",
    task: "Complete the method call that searches for '@' within email.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string email = \"student@knust.edu.gh\";\n    if (email./*BLANK*/('@') != string::npos) {\n        cout << \"Contains @\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["find"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string email = \"student@knust.edu.gh\";\n    if (email.find('@') != string::npos) {\n        cout << \"Contains @\";\n    }\n    return 0;\n}",
    testCases: ["email contains '@' -> Contains @ printed", "email without '@' -> nothing printed"],
    explanation: ".find(target) searches the string and returns the index of the first match, or the special value string::npos if the target isn't found anywhere — comparing the result against npos is the standard way to check 'does this string contain X?'.",
    commonMistake: "Comparing the result of .find() to -1 instead of string::npos — npos is technically defined as the largest possible value of its unsigned type, and while it's often equal to what -1 would represent when compared as the same type, string::npos is the correct, explicit, intended constant to compare against.",
    hints: ["This method searches for a character or substring and returns its position (or a special 'not found' value).", "email.find('@')"],
    tags: ["strings", "find", "validation"],
  },
  {
    id: "cc-str-7",
    title: "Converting a string to uppercase, character by character",
    topic: "strings",
    difficulty: "advanced-beginner",
    scenario: "A voucher code system needs to normalize user input to uppercase before checking it, one character at a time.",
    task: "Complete the loop condition, using code.length() as the upper bound.",
    starterCode: "#include <iostream>\n#include <string>\n#include <cctype>\nusing namespace std;\n\nint main() {\n    string code = \"save10\";\n    for (int i = 0; i /*BLANK*/; i++) {\n        code[i] = toupper(code[i]);\n    }\n    cout << code;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["< code.length()"] }],
    solutionCode: "#include <iostream>\n#include <string>\n#include <cctype>\nusing namespace std;\n\nint main() {\n    string code = \"save10\";\n    for (int i = 0; i < code.length(); i++) {\n        code[i] = toupper(code[i]);\n    }\n    cout << code;\n    return 0;\n}",
    testCases: ["code=\"save10\" -> SAVE10"],
    explanation: "code.length() gives the exact number of characters in the (possibly changing) string, so using it directly as the loop bound correctly visits every index from 0 up to length-1 — toupper() then converts each individual character in place.",
    commonMistake: "Hardcoding a fixed number like i < 6 instead of i < code.length() — this happens to work for this exact string, but breaks the moment the input string is a different length.",
    hints: ["Use the string's own length method as the loop's upper bound, rather than a hardcoded number.", "i < code.length()"],
    tags: ["strings", "loops", "toupper"],
  },
  {
    id: "cc-str-8",
    title: "Checking if a string is empty",
    topic: "strings",
    difficulty: "beginner",
    scenario: "A form validation check needs to make sure a required 'notes' field wasn't left blank.",
    task: "Complete the condition that checks whether notes is empty.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string notes = \"\";\n    if (notes./*BLANK*/()) {\n        cout << \"This field is required\";\n    }\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["empty"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string notes = \"\";\n    if (notes.empty()) {\n        cout << \"This field is required\";\n    }\n    return 0;\n}",
    testCases: ["notes=\"\" -> This field is required", "notes=\"looks good\" -> nothing printed"],
    explanation: ".empty() returns true when a string contains zero characters — it's the clearest, most idiomatic way to check for a blank string, more direct than comparing notes.length() == 0.",
    commonMistake: "Comparing notes == NULL — std::string is never NULL (that's a concept for raw pointers/C-strings); an empty std::string is a valid string object with zero length, checked with .empty() or .length() == 0.",
    hints: ["There's a method that directly answers 'is this string empty?'.", "notes.empty()"],
    tags: ["strings", "validation", "empty"],
  },

  // ================= M. Functions (13) =================
  {
    id: "cc-fn-1",
    title: "Declaring a simple function",
    topic: "functions",
    difficulty: "beginner",
    scenario: "A calculator app needs a reusable function that adds two numbers and returns the result.",
    task: "Complete the function's return type.",
    starterCode: "#include <iostream>\nusing namespace std;\n\n/*BLANK*/ add(int a, int b) {\n    return a + b;\n}\n\nint main() {\n    cout << add(3, 4);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["int"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint add(int a, int b) {\n    return a + b;\n}\n\nint main() {\n    cout << add(3, 4);\n    return 0;\n}",
    testCases: ["add(3, 4) -> 7"],
    explanation: "A function's return type must match the type of value its return statement gives back — since a + b (two ints) produces an int, the function is declared to return int.",
    commonMistake: "Declaring the function void while still trying to return a + b — void means 'returns nothing', so a return statement providing a value in a void function is a compile error.",
    hints: ["What type of value does a + b (two ints) produce?", "int add(int a, int b)"],
    tags: ["functions", "return-type"],
  },
  {
    id: "cc-fn-2",
    title: "A function with no return value",
    topic: "functions",
    difficulty: "beginner",
    scenario: "A logging function just prints a message and doesn't need to give any value back to the caller.",
    task: "Complete the return type for a function that returns nothing.",
    starterCode: "#include <iostream>\nusing namespace std;\n\n/*BLANK*/ logMessage(string msg) {\n    cout << \"[LOG] \" << msg;\n}\n\nint main() {\n    logMessage(\"Started\");\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["void"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nvoid logMessage(string msg) {\n    cout << \"[LOG] \" << msg;\n}\n\nint main() {\n    logMessage(\"Started\");\n    return 0;\n}",
    testCases: ["logMessage(\"Started\") -> prints: [LOG] Started"],
    explanation: "void is the return type used for functions that don't produce a value to give back to their caller — they perform an action (like printing) but there's nothing to 'return'.",
    commonMistake: "Trying to write 'return someValue;' inside a void function — a void function may only use a bare 'return;' with no value, or no return statement at all.",
    hints: ["This function doesn't give any value back — what return type means 'nothing'?", "void logMessage(string msg)"],
    tags: ["functions", "void"],
  },
  {
    id: "cc-fn-3",
    title: "Calling a function with arguments",
    topic: "functions",
    difficulty: "beginner",
    scenario: "A discount calculator function is already written; you just need to call it correctly with a price and a discount percentage.",
    task: "Complete the function call, passing 100 and 20 as arguments.",
    starterCode: "#include <iostream>\nusing namespace std;\n\ndouble applyDiscount(double price, double percent) {\n    return price - (price * percent / 100);\n}\n\nint main() {\n    cout << /*BLANK*/;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["applyDiscount(100, 20)"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\ndouble applyDiscount(double price, double percent) {\n    return price - (price * percent / 100);\n}\n\nint main() {\n    cout << applyDiscount(100, 20);\n    return 0;\n}",
    testCases: ["applyDiscount(100, 20) -> 80"],
    explanation: "Calling a function means writing its name followed by parentheses containing the arguments, in the same order as the function's parameters — applyDiscount(100, 20) passes 100 as price and 20 as percent.",
    commonMistake: "Swapping the argument order — applyDiscount(20, 100) would pass 20 as the price and 100 as the percent, giving a completely different (and nonsensical) result.",
    hints: ["Match the argument order to the parameter order: price first, then percent.", "applyDiscount(100, 20)"],
    tags: ["functions", "function-call"],
  },
  {
    id: "cc-fn-4",
    title: "Default parameter value",
    topic: "functions",
    difficulty: "intermediate",
    scenario: "A greeting function should default to a generic greeting if no custom name is provided.",
    task: "Complete the default value for the name parameter.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nvoid greet(string name /*BLANK*/) {\n    cout << \"Hello, \" << name;\n}\n\nint main() {\n    greet();\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["= \"Guest\""] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nvoid greet(string name = \"Guest\") {\n    cout << \"Hello, \" << name;\n}\n\nint main() {\n    greet();\n    return 0;\n}",
    testCases: ["greet() -> Hello, Guest", "greet(\"Ama\") -> Hello, Ama"],
    explanation: "A default parameter value (= \"Guest\") is used automatically when the caller omits that argument — greet() with no arguments uses \"Guest\", while greet(\"Ama\") overrides it with the explicitly passed value.",
    commonMistake: "Providing a default value for an earlier parameter but not for later ones (in a multi-parameter function) — in C++, once one parameter has a default, every parameter after it must also have one.",
    hints: ["A default value is assigned right in the parameter list, using =.", "string name = \"Guest\""],
    tags: ["functions", "default-parameters"],
  },
  {
    id: "cc-fn-5",
    title: "Function prototype (forward declaration)",
    topic: "functions",
    difficulty: "intermediate",
    scenario: "A program calls a function before its full definition appears later in the file, so it needs a prototype above main().",
    task: "Complete the function prototype, ending with a semicolon instead of a body.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint square(int n)/*BLANK*/\n\nint main() {\n    cout << square(5);\n    return 0;\n}\n\nint square(int n) {\n    return n * n;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: [";"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint square(int n);\n\nint main() {\n    cout << square(5);\n    return 0;\n}\n\nint square(int n) {\n    return n * n;\n}",
    testCases: ["square(5) -> 25"],
    explanation: "A function prototype declares a function's signature (name, return type, parameters) without its body, ending in a semicolon — this tells the compiler the function exists so it can be called from code appearing above its full definition.",
    commonMistake: "Writing out the full body in the prototype location (duplicating the function) — a prototype should only be the signature plus a semicolon; the body belongs in the one, separate definition.",
    hints: ["A prototype looks just like a function's first line, but ends immediately instead of opening a body.", "int square(int n);"],
    tags: ["functions", "prototype", "forward-declaration"],
  },
  {
    id: "cc-fn-6",
    title: "Function overloading by parameter type",
    topic: "functions",
    difficulty: "advanced-beginner",
    scenario: "A print utility should handle both int and double values, using two functions with the same name but different parameter types.",
    task: "Complete the second overload's parameter type.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nvoid display(int value) {\n    cout << \"Int: \" << value;\n}\n\nvoid display(/*BLANK*/ value) {\n    cout << \"Double: \" << value;\n}\n\nint main() {\n    display(3.5);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["double"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nvoid display(int value) {\n    cout << \"Int: \" << value;\n}\n\nvoid display(double value) {\n    cout << \"Double: \" << value;\n}\n\nint main() {\n    display(3.5);\n    return 0;\n}",
    testCases: ["display(3.5) -> Double: 3.5 (calls the double overload)", "display(7) -> Int: 7 (calls the int overload)"],
    explanation: "Function overloading lets multiple functions share the same name as long as their parameter types differ — the compiler picks the correct overload to call based on the type of the argument provided at each call site.",
    commonMistake: "Giving both overloads identical parameter types (or only differing in return type) — C++ distinguishes overloads by their parameter list, not by return type alone; two functions differing only in return type is a compile error.",
    hints: ["The other overload needs a different parameter type so the compiler can tell them apart.", "void display(double value)"],
    tags: ["functions", "overloading"],
  },
  {
    id: "cc-fn-7",
    title: "Recursive function: factorial",
    topic: "functions",
    difficulty: "intermediate",
    scenario: "A combinatorics tool needs a recursive factorial function, which calls itself with a smaller input until it reaches a base case.",
    task: "Complete the recursive call for the general case.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint factorial(int n) {\n    if (n <= 1) return 1;\n    return n * /*BLANK*/;\n}\n\nint main() {\n    cout << factorial(5);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["factorial(n - 1)"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint factorial(int n) {\n    if (n <= 1) return 1;\n    return n * factorial(n - 1);\n}\n\nint main() {\n    cout << factorial(5);\n    return 0;\n}",
    testCases: ["factorial(5) -> 120", "factorial(1) -> 1 (base case)"],
    explanation: "A recursive function calls itself with input that moves closer to the base case — factorial(n-1) reduces n by 1 each call, guaranteeing the chain eventually reaches n <= 1, where the recursion stops and unwinds, multiplying results back up.",
    commonMistake: "Calling factorial(n) instead of factorial(n - 1) — this would call the function with the same, never-shrinking value forever, causing infinite recursion and a stack overflow crash.",
    hints: ["The recursive call must use a smaller value than n, so it eventually reaches the base case.", "factorial(n - 1)"],
    tags: ["functions", "recursion"],
  },
  {
    id: "cc-fn-8",
    title: "Passing by value doesn't change the original",
    topic: "functions",
    difficulty: "intermediate",
    scenario: "A student is confused why calling a function to 'double' a number doesn't change the variable back in main().",
    task: "Complete the function body so it actually changes the local copy (demonstrating pass-by-value's limits).",
    starterCode: "#include <iostream>\nusing namespace std;\n\nvoid doubleIt(int n) {\n    /*BLANK*/;\n}\n\nint main() {\n    int x = 5;\n    doubleIt(x);\n    cout << x;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["n = n * 2", "n *= 2"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nvoid doubleIt(int n) {\n    n = n * 2;\n}\n\nint main() {\n    int x = 5;\n    doubleIt(x);\n    cout << x;\n    return 0;\n}",
    testCases: ["x=5 -> after doubleIt(x), x still prints 5 (unchanged!) because n is a separate copy"],
    explanation: "By default, C++ passes arguments by value — the parameter n is a completely separate copy of x's value. Changing n inside the function only affects that local copy; x in main() remains 5 no matter what doubleIt does to n. (To actually modify the caller's variable, the parameter would need to be a reference, int&.)",
    commonMistake: "Expecting x in main() to become 10 after this call — that's a very common misunderstanding of pass-by-value; without a reference parameter, the original variable in the caller is never touched.",
    hints: ["This just needs to double the local parameter n — it will NOT affect x in main(), which is the whole point of this example.", "n = n * 2;"],
    tags: ["functions", "pass-by-value", "common-bug"],
  },
  {
    id: "cc-fn-9",
    title: "Returning a boolean from a validity check",
    topic: "functions",
    difficulty: "beginner",
    scenario: "A form validator function checks whether an age value is realistic (between 0 and 120).",
    task: "Complete the return statement using a comparison expression as the returned value.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nbool isValidAge(int age) {\n    return /*BLANK*/;\n}\n\nint main() {\n    cout << isValidAge(200);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["age >= 0 && age <= 120"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nbool isValidAge(int age) {\n    return age >= 0 && age <= 120;\n}\n\nint main() {\n    cout << isValidAge(200);\n    return 0;\n}",
    testCases: ["isValidAge(200) -> 0 (false)", "isValidAge(30) -> 1 (true)"],
    explanation: "A boolean expression can be returned directly — age >= 0 && age <= 120 already evaluates to true or false on its own, so there's no need for an if-statement wrapper; the expression's result IS the function's return value.",
    commonMistake: "Writing an unnecessary if-else just to return true or false explicitly (if (age >= 0 && age <= 120) return true; else return false;) — this works but is more verbose than simply returning the boolean expression directly.",
    hints: ["The condition itself already produces a true/false value — you can return it directly.", "return age >= 0 && age <= 120;"],
    tags: ["functions", "bool", "validation"],
  },
  {
    id: "cc-fn-10",
    title: "A function that calls another function",
    topic: "functions",
    difficulty: "intermediate",
    scenario: "A total-cost function needs to call an already-defined tax-calculation function internally.",
    task: "Complete the call to calculateTax from within calculateTotal.",
    starterCode: "#include <iostream>\nusing namespace std;\n\ndouble calculateTax(double amount) {\n    return amount * 0.15;\n}\n\ndouble calculateTotal(double amount) {\n    return amount + /*BLANK*/;\n}\n\nint main() {\n    cout << calculateTotal(100);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["calculateTax(amount)"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\ndouble calculateTax(double amount) {\n    return amount * 0.15;\n}\n\ndouble calculateTotal(double amount) {\n    return amount + calculateTax(amount);\n}\n\nint main() {\n    cout << calculateTotal(100);\n    return 0;\n}",
    testCases: ["calculateTotal(100) -> 115"],
    explanation: "Functions can freely call other already-defined functions — calculateTotal reuses calculateTax rather than re-implementing the tax formula itself, which avoids duplicating logic and keeps each function focused on one job.",
    commonMistake: "Re-writing the tax formula inline inside calculateTotal (amount + amount * 0.15) instead of calling calculateTax — this works but duplicates logic that would then need to be updated in two places if the tax rate ever changes.",
    hints: ["Reuse the existing calculateTax function instead of repeating its formula.", "calculateTax(amount)"],
    tags: ["functions", "composition"],
  },
  {
    id: "cc-fn-11",
    title: "Function with multiple parameters, mixed types",
    topic: "functions",
    difficulty: "beginner",
    scenario: "A receipt-printing function needs an item name (string), a quantity (int), and a unit price (double).",
    task: "Complete the parameter list with the correct types in order.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nvoid printReceipt(/*BLANK*/) {\n    cout << name << \": \" << qty << \" x \" << price;\n}\n\nint main() {\n    printReceipt(\"Bread\", 2, 5.5);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["string name, int qty, double price"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nvoid printReceipt(string name, int qty, double price) {\n    cout << name << \": \" << qty << \" x \" << price;\n}\n\nint main() {\n    printReceipt(\"Bread\", 2, 5.5);\n    return 0;\n}",
    testCases: ["printReceipt(\"Bread\", 2, 5.5) -> Bread: 2 x 5.5"],
    explanation: "A function's parameters can mix different types freely, each declared with its own type — string name, int qty, double price matches the call's argument types and order exactly (\"Bread\" is a string, 2 is an int, 5.5 is a double).",
    commonMistake: "Getting the parameter order wrong relative to how the function is called — since arguments are matched positionally (not by name), swapping the order in either the declaration or the call site breaks the correspondence.",
    hints: ["Three parameters, matching the three arguments' types and order in the call.", "string name, int qty, double price"],
    tags: ["functions", "parameters"],
  },
  {
    id: "cc-fn-12",
    title: "Early return to exit a function",
    topic: "functions",
    difficulty: "advanced-beginner",
    scenario: "A withdrawal function should immediately stop and report failure if the amount requested is negative, without executing the rest of its logic.",
    task: "Complete the early-return guard clause at the top of the function.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nstring processWithdrawal(double amount) {\n    if (amount < 0) {\n        /*BLANK*/ \"Invalid amount\";\n    }\n    return \"Processed\";\n}\n\nint main() {\n    cout << processWithdrawal(-50);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["return"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nstring processWithdrawal(double amount) {\n    if (amount < 0) {\n        return \"Invalid amount\";\n    }\n    return \"Processed\";\n}\n\nint main() {\n    cout << processWithdrawal(-50);\n    return 0;\n}",
    testCases: ["processWithdrawal(-50) -> Invalid amount (returns immediately, skips the rest)", "processWithdrawal(50) -> Processed"],
    explanation: "return immediately exits a function and hands back the given value — using it inside an early guard clause (like this negative-amount check) stops the rest of the function's logic from running at all for invalid input.",
    commonMistake: "Using cout to print the error message instead of return — that would print 'Invalid amount' AND still fall through to also return 'Processed', producing confusing double output.",
    hints: ["You need to exit the function immediately, handing back this string as the result.", "return \"Invalid amount\";"],
    tags: ["functions", "early-return", "guard-clause"],
  },
  {
    id: "cc-fn-13",
    title: "Function returning the larger of two values",
    topic: "functions",
    difficulty: "beginner",
    scenario: "A pricing tool needs a reusable function that returns whichever of two prices is higher.",
    task: "Complete the ternary expression used as the return value.",
    starterCode: "#include <iostream>\nusing namespace std;\n\ndouble getMax(double a, double b) {\n    return /*BLANK*/;\n}\n\nint main() {\n    cout << getMax(12.5, 9.75);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["a > b ? a : b"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\ndouble getMax(double a, double b) {\n    return a > b ? a : b;\n}\n\nint main() {\n    cout << getMax(12.5, 9.75);\n    return 0;\n}",
    testCases: ["getMax(12.5, 9.75) -> 12.5"],
    explanation: "The ternary operator a > b ? a : b directly produces whichever value is larger as a single expression, which can be returned immediately — a common, concise pattern for simple two-way comparisons inside a function.",
    commonMistake: "Writing 'return a > b;' by itself — that returns a bool (true/false) indicating whether a is bigger, not the actual larger VALUE, which is a completely different (and wrong) result for this function's purpose.",
    hints: ["A ternary expression picks between a and b based on the comparison.", "a > b ? a : b"],
    tags: ["functions", "ternary"],
  },
  {
    id: "cc-fn-14",
    title: "Checking whether a student's registration number is even",
    topic: "functions",
    difficulty: "beginner",
    scenario: "A university's exam hall allocator sends students with an even registration number to Hall A and odd ones to Hall B, using a reusable helper function.",
    task: "Complete the function body that returns whether n is even.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nbool isEven(int n) {\n    /*BLANK*/\n}\n\nint main() {\n    cout << isEven(20345);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["return n % 2 == 0;", "return n % 2 == 0"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nbool isEven(int n) {\n    return n % 2 == 0;\n}\n\nint main() {\n    cout << isEven(20345);\n    return 0;\n}",
    testCases: ["isEven(20345) -> false (prints 0)", "isEven(20344) -> true (prints 1)"],
    explanation: "n % 2 gives the remainder after dividing by 2, which is 0 for every even number — comparing that to 0 with == produces the true/false result directly, which return then hands straight back to the caller as the function's bool result.",
    commonMistake: "Writing 'return n % 2;' without the == 0 comparison — this happens to also work as a boolean (0 is falsy, 1 is truthy) but backwards: it evaluates truthy for ODD numbers, not even ones, which is the opposite of what isEven should report.",
    hints: ["You need a full return statement, not just an expression fragment.", "return n % 2 == 0;"],
    tags: ["functions", "algorithm", "student-management"],
  },

  // ================= N. References (4) =================
  {
    id: "cc-ref-1",
    title: "Passing by reference to modify the original",
    topic: "references",
    difficulty: "intermediate",
    scenario: "A deposit function should actually change the caller's balance variable, not just a local copy.",
    task: "Complete the parameter type so balance is passed by reference.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nvoid deposit(double /*BLANK*/ balance, double amount) {\n    balance += amount;\n}\n\nint main() {\n    double accountBalance = 100;\n    deposit(accountBalance, 50);\n    cout << accountBalance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["&"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nvoid deposit(double &balance, double amount) {\n    balance += amount;\n}\n\nint main() {\n    double accountBalance = 100;\n    deposit(accountBalance, 50);\n    cout << accountBalance;\n    return 0;\n}",
    testCases: ["accountBalance=100, deposit 50 -> accountBalance becomes 150 (the original IS changed)"],
    explanation: "The & after the type makes balance a reference parameter — an alias for the caller's actual variable, not a copy. Changes made to balance inside the function directly affect accountBalance back in main(), unlike ordinary pass-by-value.",
    commonMistake: "Omitting the & and expecting the caller's variable to change anyway — without it, balance is just an ordinary copy (pass-by-value), and accountBalance in main() would remain 100 no matter what the function does.",
    hints: ["You need a symbol after the type that makes this parameter an alias for the caller's variable.", "double &balance"],
    tags: ["references", "pass-by-reference", "fintech"],
  },
  {
    id: "cc-ref-2",
    title: "A reference variable as an alias",
    topic: "references",
    difficulty: "beginner",
    scenario: "A program needs a second name, total, that refers to the exact same memory as an existing variable, sum.",
    task: "Complete the reference declaration.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int sum = 50;\n    int /*BLANK*/ total = sum;\n    total = 100;\n    cout << sum;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["&"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int sum = 50;\n    int &total = sum;\n    total = 100;\n    cout << sum;\n    return 0;\n}",
    testCases: ["sum=50 initially; after total=100, sum also prints 100, since total IS sum"],
    explanation: "int &total = sum; makes total a reference — another name for the exact same variable as sum, not a separate copy. Any change made through total is really a change to sum itself, which is why sum prints 100 after total is set to 100.",
    commonMistake: "Confusing a reference with a regular copy (int total = sum;, no &) — with a plain copy, changing total afterward would NOT affect sum at all; the & is what creates the aliasing relationship.",
    hints: ["A reference is declared with a specific symbol right after the type.", "int &total = sum;"],
    tags: ["references", "aliasing"],
  },
  {
    id: "cc-ref-3",
    title: "Reference parameter to avoid copying a large object",
    topic: "references",
    difficulty: "advanced-beginner",
    scenario: "A function that just reads (doesn't modify) a long customer record string should avoid copying it every call, using a const reference.",
    task: "Complete the parameter declaration as a const reference.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nvoid printRecord(/*BLANK*/ string &record) {\n    cout << record;\n}\n\nint main() {\n    string customer = \"Ama Serwaa, Acct #4471\";\n    printRecord(customer);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["const"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nvoid printRecord(const string &record) {\n    cout << record;\n}\n\nint main() {\n    string customer = \"Ama Serwaa, Acct #4471\";\n    printRecord(customer);\n    return 0;\n}",
    testCases: ["printRecord(customer) -> Ama Serwaa, Acct #4471"],
    explanation: "const string &record avoids copying the string (the & makes it a reference to the original) while const guarantees the function cannot accidentally modify the caller's data — the standard, efficient way to pass a read-only large object into a function.",
    commonMistake: "Passing by plain value (string record, no & at all) for a large object purely for reading — this silently copies the entire string on every call, which is wasteful, especially for large strings or objects, compared to a const reference.",
    hints: ["This keyword, combined with the reference, guarantees the function can't modify the original.", "const string &record"],
    tags: ["references", "const", "efficiency"],
  },
  {
    id: "cc-ref-4",
    title: "Swapping two values using references",
    topic: "references",
    difficulty: "intermediate",
    scenario: "A sorting helper function swaps two int variables' values using reference parameters so the change is visible back in main().",
    task: "Complete the parameter list so both a and b are references.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nvoid swapValues(int /*BLANK1*/ a, int /*BLANK2*/ b) {\n    int temp = a;\n    a = b;\n    b = temp;\n}\n\nint main() {\n    int x = 5, y = 10;\n    swapValues(x, y);\n    cout << x << \" \" << y;\n    return 0;\n}",
    blanks: [
      { id: "/*BLANK1*/", kind: "inline", acceptableAnswers: ["&"] },
      { id: "/*BLANK2*/", kind: "inline", acceptableAnswers: ["&"] },
    ],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nvoid swapValues(int &a, int &b) {\n    int temp = a;\n    a = b;\n    b = temp;\n}\n\nint main() {\n    int x = 5, y = 10;\n    swapValues(x, y);\n    cout << x << \" \" << y;\n    return 0;\n}",
    testCases: ["x=5, y=10 -> after swapValues(x, y): x=10 y=5"],
    explanation: "Both parameters need & to be references, since the whole point of swapValues is to modify BOTH of the caller's original variables — if either parameter were passed by value instead, that one variable's swap wouldn't be visible back in main().",
    commonMistake: "Making only one parameter a reference (e.g. int &a, int b) — this would successfully update x but leave y completely unchanged, producing a broken, half-working swap.",
    hints: ["Both a and b need to be references, not just one.", "int &a, int &b"],
    tags: ["references", "swap", "pass-by-reference"],
  },

  // ================= O. Pointers (6) =================
  {
    id: "cc-ptr-1",
    title: "Declaring a pointer",
    topic: "pointers",
    difficulty: "intermediate",
    scenario: "A program needs a pointer variable that will store the memory address of an int.",
    task: "Complete the pointer declaration syntax.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int balance = 500;\n    int /*BLANK*/ ptr = &balance;\n    cout << *ptr;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["*"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int balance = 500;\n    int *ptr = &balance;\n    cout << *ptr;\n    return 0;\n}",
    testCases: ["Output: 500"],
    explanation: "int *ptr declares ptr as a pointer to an int — it stores a memory address rather than a value directly. &balance takes the address of balance, and *ptr later dereferences the pointer to access the value stored at that address (500).",
    commonMistake: "Confusing the * in the declaration (which means 'this variable is a pointer') with the * used later to dereference (which means 'the value at this address') — they look identical but mean different things depending on context.",
    hints: ["The symbol that marks a variable as a pointer goes right before its name.", "int *ptr = &balance;"],
    tags: ["pointers", "declaration"],
  },
  {
    id: "cc-ptr-2",
    title: "Getting the address of a variable",
    topic: "pointers",
    difficulty: "intermediate",
    scenario: "A debugging tool needs to print the actual memory address where a variable is stored.",
    task: "Complete the expression that gets the address of score.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int score = 88;\n    int *ptr = /*BLANK*/;\n    cout << (ptr != nullptr);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["&score"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int score = 88;\n    int *ptr = &score;\n    cout << (ptr != nullptr);\n    return 0;\n}",
    testCases: ["ptr correctly holds score's address -> (ptr != nullptr) is true, prints 1"],
    explanation: "The & (address-of) operator retrieves the memory address where a variable is stored — &score gives the address of score, which is exactly the kind of value a pointer variable is designed to hold.",
    commonMistake: "Writing int *ptr = score; (without &) — that tries to store score's actual VALUE (88) into a pointer, which expects an address, not a value; this is a type mismatch the compiler will reject.",
    hints: ["You need the address-of operator applied to score.", "&score"],
    tags: ["pointers", "address-of"],
  },
  {
    id: "cc-ptr-3",
    title: "Dereferencing to modify the pointed-to value",
    topic: "pointers",
    difficulty: "intermediate",
    scenario: "A function has a pointer to a balance variable and needs to update the actual balance through the pointer.",
    task: "Complete the dereference-and-assign statement.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 100;\n    double *ptr = &balance;\n    /*BLANK*/ = 250;\n    cout << balance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["*ptr"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double balance = 100;\n    double *ptr = &balance;\n    *ptr = 250;\n    cout << balance;\n    return 0;\n}",
    testCases: ["balance=100 initially; after *ptr = 250, balance also prints 250"],
    explanation: "*ptr (dereferencing) accesses the value AT the address ptr holds — since ptr holds balance's address, *ptr = 250 writes 250 directly into balance's memory, which is why balance itself changes.",
    commonMistake: "Writing 'ptr = 250;' without the * — that would attempt to make ptr point to the (nonsensical, essentially garbage) memory address 250, rather than changing the value balance actually holds.",
    hints: ["You need to dereference the pointer before assigning, to modify the value it points to, not the pointer itself.", "*ptr = 250;"],
    tags: ["pointers", "dereference"],
  },
  {
    id: "cc-ptr-4",
    title: "A null pointer before assignment",
    topic: "pointers",
    difficulty: "advanced-beginner",
    scenario: "A pointer is declared before it's ready to point at anything real, and should be explicitly set to a safe 'points at nothing' state.",
    task: "Complete the initialization so accountPtr safely points at nothing yet.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int *accountPtr = /*BLANK*/;\n    cout << (accountPtr == nullptr);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["nullptr"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int *accountPtr = nullptr;\n    cout << (accountPtr == nullptr);\n    return 0;\n}",
    testCases: ["Output: 1 (true — the pointer is null)"],
    explanation: "nullptr is the modern C++ keyword representing a pointer that intentionally points at nothing — assigning it explicitly avoids the danger of an uninitialized pointer holding a random, garbage address that could crash the program if dereferenced.",
    commonMistake: "Leaving a pointer uninitialized (int *accountPtr;  with nothing assigned) — unlike nullptr, an uninitialized pointer holds an unpredictable garbage address, and dereferencing it is undefined behavior, not a safe, detectable 'empty' state.",
    hints: ["This is the modern C++ keyword for 'a pointer that points at nothing'.", "nullptr"],
    tags: ["pointers", "nullptr", "safety"],
  },
  {
    id: "cc-ptr-5",
    title: "Pointer arithmetic to walk through an array",
    topic: "pointers",
    difficulty: "advanced-beginner",
    scenario: "A low-level routine walks through an array of prices using a pointer instead of an index variable.",
    task: "Complete the pointer increment that moves to the next element.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double prices[3] = {10, 20, 30};\n    double *ptr = prices;\n    cout << *ptr << \" \";\n    /*BLANK*/;\n    cout << *ptr;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["ptr++"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double prices[3] = {10, 20, 30};\n    double *ptr = prices;\n    cout << *ptr << \" \";\n    ptr++;\n    cout << *ptr;\n    return 0;\n}",
    testCases: ["Prints: 10 20  (ptr moves from prices[0] to prices[1])"],
    explanation: "ptr++ advances the pointer to the next element of the array it points into — the array's name (prices) itself decays to a pointer to its first element, and incrementing a pointer moves it forward by exactly one element of its pointed-to type, not just one byte.",
    commonMistake: "Assuming ptr++ moves forward by exactly 1 byte regardless of type — pointer arithmetic actually scales by the size of the pointed-to type (here, sizeof(double)), so ptr++ correctly lands on the next double-sized element, not the next byte.",
    hints: ["This is the same increment operator used on ordinary variables, applied to the pointer itself.", "ptr++;"],
    tags: ["pointers", "pointer-arithmetic", "arrays"],
  },
  {
    id: "cc-ptr-6",
    title: "Passing a pointer to modify a caller's variable",
    topic: "pointers",
    difficulty: "advanced-beginner",
    scenario: "A function uses a pointer parameter (an alternative to references) to double the caller's variable.",
    task: "Complete the dereference used inside the function to access/modify the pointed-to value.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nvoid doubleValue(int *n) {\n    /*BLANK1*/ = /*BLANK2*/ * 2;\n}\n\nint main() {\n    int x = 7;\n    doubleValue(&x);\n    cout << x;\n    return 0;\n}",
    blanks: [
      { id: "/*BLANK1*/", kind: "inline", acceptableAnswers: ["*n"] },
      { id: "/*BLANK2*/", kind: "inline", acceptableAnswers: ["*n"] },
    ],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nvoid doubleValue(int *n) {\n    *n = *n * 2;\n}\n\nint main() {\n    int x = 7;\n    doubleValue(&x);\n    cout << x;\n    return 0;\n}",
    testCases: ["x=7 -> after doubleValue(&x), x prints 14"],
    explanation: "Passing &x gives the function the address of x, stored in the pointer parameter n. Dereferencing with *n both reads and writes the actual value at that address, so *n = *n * 2 doubles x itself — a pointer-based alternative to a reference parameter for achieving the same effect.",
    commonMistake: "Writing 'n = n * 2;' (without dereferencing) — that would try to do arithmetic on the pointer's address value itself, not the int it points to, which is both semantically wrong and a type error against a plain int multiplication.",
    hints: ["Both sides of this assignment need the dereference operator, to work with the pointed-to value, not the pointer itself.", "*n = *n * 2;"],
    tags: ["pointers", "dereference", "functions"],
  },

  // ================= P. Dynamic Memory (4) =================
  {
    id: "cc-dm-1",
    title: "Allocating a single int on the heap",
    topic: "dynamic-memory",
    difficulty: "advanced-beginner",
    scenario: "A program needs to allocate memory for a single int at runtime, rather than using a regular stack variable.",
    task: "Complete the allocation expression.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int *ptr = /*BLANK*/ int;\n    *ptr = 42;\n    cout << *ptr;\n    delete ptr;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["new"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int *ptr = new int;\n    *ptr = 42;\n    cout << *ptr;\n    delete ptr;\n    return 0;\n}",
    testCases: ["Output: 42"],
    explanation: "new int allocates memory for one int on the heap and returns a pointer to it — heap memory persists until it's explicitly freed (unlike stack variables, which are automatically cleaned up when they go out of scope).",
    commonMistake: "Forgetting the matching delete at the end — every new must be paired with exactly one delete, or the allocated memory is never freed (a memory leak).",
    hints: ["This keyword requests memory from the heap at runtime.", "new int"],
    tags: ["dynamic-memory", "new", "heap"],
  },
  {
    id: "cc-dm-2",
    title: "Freeing heap memory",
    topic: "dynamic-memory",
    difficulty: "advanced-beginner",
    scenario: "A program that allocated memory with new needs to release it back to the system once it's no longer needed.",
    task: "Complete the statement that frees the memory pointed to by ptr.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int *ptr = new int(99);\n    cout << *ptr;\n    /*BLANK*/ ptr;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["delete"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int *ptr = new int(99);\n    cout << *ptr;\n    delete ptr;\n    return 0;\n}",
    testCases: ["Output: 99, then the allocated memory is properly freed"],
    explanation: "delete releases heap memory previously obtained with new, returning it to the system so it can be reused — every new must eventually be matched with exactly one delete to avoid a memory leak.",
    commonMistake: "Calling delete twice on the same pointer (double-free) — this causes undefined behavior; a pointer should be deleted exactly once, ideally set to nullptr afterward to avoid accidental reuse.",
    hints: ["This keyword releases memory that was allocated with new.", "delete ptr;"],
    tags: ["dynamic-memory", "delete", "memory-leak"],
  },
  {
    id: "cc-dm-3",
    title: "Allocating a dynamic array",
    topic: "dynamic-memory",
    difficulty: "advanced-beginner",
    scenario: "A program doesn't know how many scores it needs to store until runtime, so it allocates the array dynamically based on a variable, count.",
    task: "Complete the dynamic array allocation using new[].",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int count = 4;\n    int *scores = new int/*BLANK*/;\n    scores[0] = 10;\n    cout << scores[0];\n    delete[] scores;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["[count]"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int count = 4;\n    int *scores = new int[count];\n    scores[0] = 10;\n    cout << scores[0];\n    delete[] scores;\n    return 0;\n}",
    testCases: ["count=4 -> scores can hold 4 ints; scores[0] prints 10"],
    explanation: "new int[count] allocates an array of count ints on the heap, whose size can be a runtime variable (unlike a regular stack array, whose size must generally be known at compile time) — the pointer returned can then be indexed with [] just like an ordinary array.",
    commonMistake: "Freeing a dynamic array with plain delete instead of delete[] — arrays allocated with new[] must be freed with the matching delete[], or the behavior is undefined (it may not properly destroy every element).",
    hints: ["Use square brackets with the runtime count to allocate an array, not a single value.", "new int[count]"],
    tags: ["dynamic-memory", "dynamic-array", "new"],
  },
  {
    id: "cc-dm-4",
    title: "Freeing a dynamic array correctly",
    topic: "dynamic-memory",
    difficulty: "advanced-beginner",
    scenario: "A dynamically allocated array of temperatures needs to be freed with the array-specific delete form.",
    task: "Complete the correct deallocation for an array (not a single value).",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int *temps = new int[5];\n    /*BLANK*/ temps;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["delete[]"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int *temps = new int[5];\n    delete[] temps;\n    return 0;\n}",
    testCases: ["Memory allocated by new int[5] is correctly and fully freed"],
    explanation: "Array allocations (new T[n]) must always be paired with delete[], not plain delete — the [] tells the runtime it's dealing with an array so it can correctly clean up every element, not just the first one.",
    commonMistake: "Using plain 'delete temps;' (missing the brackets) to free an array allocated with new[] — this mismatch is undefined behavior, and different compilers/runtimes may fail in different, unpredictable ways.",
    hints: ["Since this was allocated as an array (with brackets), it must be freed with the matching bracketed form.", "delete[] temps;"],
    tags: ["dynamic-memory", "delete", "arrays"],
  },

  // ================= Q. Structs (6) =================
  {
    id: "cc-st-1",
    title: "Defining a struct",
    topic: "structs",
    difficulty: "intermediate",
    scenario: "A student records system needs a simple struct grouping a name and a score together.",
    task: "Complete the struct keyword and its members.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\n/*BLANK*/ Student {\n    string name;\n    int score;\n};\n\nint main() {\n    Student s;\n    s.name = \"Kojo\";\n    s.score = 85;\n    cout << s.name << \" \" << s.score;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["struct"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nstruct Student {\n    string name;\n    int score;\n};\n\nint main() {\n    Student s;\n    s.name = \"Kojo\";\n    s.score = 85;\n    cout << s.name << \" \" << s.score;\n    return 0;\n}",
    testCases: ["Output: Kojo 85"],
    explanation: "struct groups multiple related pieces of data (here, name and score) into a single custom type — creating a variable of type Student then bundles both fields together, accessed with dot notation (s.name, s.score).",
    commonMistake: "Forgetting the semicolon after the struct's closing brace — unlike a function body, a struct/class definition must end with ; after its closing }.",
    hints: ["This keyword defines a new custom data type grouping several fields together.", "struct Student { ... };"],
    tags: ["structs", "declaration"],
  },
  {
    id: "cc-st-2",
    title: "Accessing struct members with dot notation",
    topic: "structs",
    difficulty: "beginner",
    scenario: "A struct already defined for a bank account needs its balance field read and printed.",
    task: "Complete the member access expression.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nstruct Account {\n    string owner;\n    double balance;\n};\n\nint main() {\n    Account acc = {\"Adwoa\", 750.0};\n    cout << acc/*BLANK*/balance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["."] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nstruct Account {\n    string owner;\n    double balance;\n};\n\nint main() {\n    Account acc = {\"Adwoa\", 750.0};\n    cout << acc.balance;\n    return 0;\n}",
    testCases: ["Output: 750"],
    explanation: "The dot operator (.) accesses a member (field) of a struct or class instance — acc.balance reaches into the acc variable and retrieves its balance field specifically.",
    commonMistake: "Using -> instead of . — the arrow operator is for accessing members through a POINTER to a struct, while . is for accessing members directly on the struct object itself (acc here is a plain Account, not a pointer).",
    hints: ["This is the standard operator for accessing a field on an object you hold directly (not through a pointer).", "acc.balance"],
    tags: ["structs", "member-access"],
  },
  {
    id: "cc-st-3",
    title: "Struct with an initializer list",
    topic: "structs",
    difficulty: "intermediate",
    scenario: "A product struct needs to be created and initialized with values in one line, matching its member order.",
    task: "Complete the brace initialization with the product's name and price, in member order.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nstruct Product {\n    string name;\n    double price;\n};\n\nint main() {\n    Product item = /*BLANK*/;\n    cout << item.name << \" costs \" << item.price;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["{\"Notebook\", 3.5}"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nstruct Product {\n    string name;\n    double price;\n};\n\nint main() {\n    Product item = {\"Notebook\", 3.5};\n    cout << item.name << \" costs \" << item.price;\n    return 0;\n}",
    testCases: ["Output: Notebook costs 3.5"],
    explanation: "A brace-enclosed initializer list ({\"Notebook\", 3.5}) fills a struct's members in the exact order they were declared — the first value goes to name, the second to price, since Product declares name before price.",
    commonMistake: "Providing the values in the wrong order relative to the struct's declared member order (e.g. {3.5, \"Notebook\"}) — this either fails to compile (type mismatch) or, in the worst case, silently assigns values to the wrong fields.",
    hints: ["Match the order of values to the order the struct's fields were declared: name first, then price.", "{\"Notebook\", 3.5}"],
    tags: ["structs", "initialization"],
  },
  {
    id: "cc-st-4",
    title: "An array of structs",
    topic: "structs",
    difficulty: "advanced-beginner",
    scenario: "A small inventory system stores multiple products, each represented by a struct, in an array.",
    task: "Complete the declaration of an array of 2 Product structs.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nstruct Product {\n    string name;\n    double price;\n};\n\nint main() {\n    Product /*BLANK*/ = {{\"Pen\", 1.5}, {\"Eraser\", 0.75}};\n    cout << items[1].name;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["items[2]"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nstruct Product {\n    string name;\n    double price;\n};\n\nint main() {\n    Product items[2] = {{\"Pen\", 1.5}, {\"Eraser\", 0.75}};\n    cout << items[1].name;\n    return 0;\n}",
    testCases: ["Output: Eraser"],
    explanation: "An array of structs works just like an array of any other type — Product items[2] declares 2 Product elements, and each element still uses dot notation to access its own fields (items[1].name reaches the second element's name).",
    commonMistake: "Trying to access a field with items.name[1] or items[1]name (missing the dot) — the correct order is: index into the array first ([1]), THEN access the member with a dot (.name).",
    hints: ["This needs both an array size (2 elements) and, later, an index plus dot notation to reach a field.", "Product items[2] = {...}; then items[1].name"],
    tags: ["structs", "arrays"],
  },
  {
    id: "cc-st-5",
    title: "Passing a struct to a function",
    topic: "structs",
    difficulty: "intermediate",
    scenario: "A function needs to receive a Student struct and print a formatted summary of it.",
    task: "Complete the function parameter type.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nstruct Student {\n    string name;\n    int score;\n};\n\nvoid printStudent(/*BLANK*/ s) {\n    cout << s.name << \": \" << s.score;\n}\n\nint main() {\n    Student s1 = {\"Yaw\", 92};\n    printStudent(s1);\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["Student"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nstruct Student {\n    string name;\n    int score;\n};\n\nvoid printStudent(Student s) {\n    cout << s.name << \": \" << s.score;\n}\n\nint main() {\n    Student s1 = {\"Yaw\", 92};\n    printStudent(s1);\n    return 0;\n}",
    testCases: ["printStudent(s1) -> Yaw: 92"],
    explanation: "A struct works as a parameter type just like any built-in type — Student s means the function receives a full copy of the struct, with all of its fields accessible through s inside the function body.",
    commonMistake: "Forgetting that passing a struct by value (as here) copies ALL of its fields — for a large struct, this can be inefficient, and a reference parameter (Student &s) would avoid the copy, though it's not required for correctness here.",
    hints: ["The parameter's type is the struct's own name.", "Student s"],
    tags: ["structs", "functions", "parameters"],
  },
  {
    id: "cc-st-6",
    title: "Nested struct: an address inside a customer record",
    topic: "structs",
    difficulty: "advanced-beginner",
    scenario: "A customer record struct contains an Address struct as one of its own fields, requiring chained dot access.",
    task: "Complete the chained member access to reach the city field.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nstruct Address {\n    string city;\n};\n\nstruct Customer {\n    string name;\n    Address location;\n};\n\nint main() {\n    Customer c;\n    c.name = \"Efua\";\n    c.location.city = \"Kumasi\";\n    cout << c/*BLANK*/city;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: [".location."] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nstruct Address {\n    string city;\n};\n\nstruct Customer {\n    string name;\n    Address location;\n};\n\nint main() {\n    Customer c;\n    c.name = \"Efua\";\n    c.location.city = \"Kumasi\";\n    cout << c.location.city;\n    return 0;\n}",
    testCases: ["Output: Kumasi"],
    explanation: "When a struct contains another struct as a field, reaching a nested field requires chaining dots: c.location gets the Address struct inside c, and .city then reaches into that nested struct — c.location.city in full.",
    commonMistake: "Trying to skip straight to c.city — that field doesn't exist directly on Customer; city belongs to the nested Address struct, reachable only via c.location.city.",
    hints: ["You need to go through the nested 'location' field first, then reach its 'city' field.", "c.location.city"],
    tags: ["structs", "nested-structs"],
  },

  // ================= R. Classes / OOP Basics (8) =================
  {
    id: "cc-cls-1",
    title: "Defining a simple class",
    topic: "classes",
    difficulty: "intermediate",
    scenario: "A banking app needs a BankAccount class with a private balance and public methods to interact with it.",
    task: "Complete the class keyword.",
    starterCode: "#include <iostream>\nusing namespace std;\n\n/*BLANK*/ BankAccount {\npublic:\n    double balance = 0;\n};\n\nint main() {\n    BankAccount acc;\n    cout << acc.balance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["class"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nclass BankAccount {\npublic:\n    double balance = 0;\n};\n\nint main() {\n    BankAccount acc;\n    cout << acc.balance;\n    return 0;\n}",
    testCases: ["Output: 0"],
    explanation: "class defines a custom type that bundles data (like balance) with the functions that operate on it — similar to struct, but with private access as the default (structs default to public), which is why the access specifiers matter more with classes.",
    commonMistake: "Forgetting the semicolon after the class's closing brace, exactly the same requirement as with struct — both need a trailing ; after their definition.",
    hints: ["This keyword defines a class, which bundles data and behavior together.", "class BankAccount { ... };"],
    tags: ["classes", "oop", "declaration"],
  },
  {
    id: "cc-cls-2",
    title: "Private members and public access",
    topic: "classes",
    difficulty: "intermediate",
    scenario: "A BankAccount class should keep its balance private, only reachable through a public getBalance() method.",
    task: "Complete the access specifier that makes balance inaccessible from outside the class.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nclass BankAccount {\n/*BLANK*/:\n    double balance = 500;\npublic:\n    double getBalance() { return balance; }\n};\n\nint main() {\n    BankAccount acc;\n    cout << acc.getBalance();\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["private"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nclass BankAccount {\nprivate:\n    double balance = 500;\npublic:\n    double getBalance() { return balance; }\n};\n\nint main() {\n    BankAccount acc;\n    cout << acc.getBalance();\n    return 0;\n}",
    testCases: ["acc.getBalance() -> 500"],
    explanation: "private: marks the members below it as inaccessible from outside the class — balance can only be read or changed by the class's own methods, like getBalance(), enforcing encapsulation. Note that class members are private by default anyway, but writing private: explicitly documents the intent clearly.",
    commonMistake: "Trying to access acc.balance directly from main() when it's private — this is a compile error ('balance is private within this context'), which is exactly the protection private is meant to provide.",
    hints: ["This access specifier hides a member from code outside the class.", "private:"],
    tags: ["classes", "encapsulation", "private"],
  },
  {
    id: "cc-cls-3",
    title: "A constructor to initialize an object",
    topic: "classes",
    difficulty: "advanced-beginner",
    scenario: "A BankAccount class should set its starting balance automatically when a new account object is created.",
    task: "Complete the constructor's name, which must exactly match the class name.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nclass BankAccount {\npublic:\n    double balance;\n    /*BLANK*/(double startingBalance) {\n        balance = startingBalance;\n    }\n};\n\nint main() {\n    BankAccount acc(1000);\n    cout << acc.balance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["BankAccount"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nclass BankAccount {\npublic:\n    double balance;\n    BankAccount(double startingBalance) {\n        balance = startingBalance;\n    }\n};\n\nint main() {\n    BankAccount acc(1000);\n    cout << acc.balance;\n    return 0;\n}",
    testCases: ["BankAccount acc(1000); -> acc.balance is 1000"],
    explanation: "A constructor is a special member function with the exact same name as the class and no return type — it runs automatically whenever a new object is created, here setting balance from the value passed in (1000).",
    commonMistake: "Giving the constructor a different name than the class, or adding a return type like void — a real constructor must be named identically to the class, with no return type at all (not even void).",
    hints: ["A constructor's name must exactly match the class's name.", "BankAccount(double startingBalance) { ... }"],
    tags: ["classes", "constructor", "oop"],
  },
  {
    id: "cc-cls-4",
    title: "Calling a member function (method)",
    topic: "classes",
    difficulty: "beginner",
    scenario: "A Calculator class has a method to add two stored numbers; the caller just needs to invoke it correctly.",
    task: "Complete the method call using dot notation.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nclass Calculator {\npublic:\n    int a = 4, b = 6;\n    int add() { return a + b; }\n};\n\nint main() {\n    Calculator calc;\n    cout << calc/*BLANK*/();\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: [".add"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nclass Calculator {\npublic:\n    int a = 4, b = 6;\n    int add() { return a + b; }\n};\n\nint main() {\n    Calculator calc;\n    cout << calc.add();\n    return 0;\n}",
    testCases: ["calc.add() -> 10"],
    explanation: "Just like accessing a data member, calling a method on an object uses dot notation followed by the method's name and parentheses — calc.add() invokes the add() method belonging to the calc object specifically.",
    commonMistake: "Forgetting the parentheses (calc.add without ()) — without them, you're referring to the method itself (its address), not actually calling it and getting its return value.",
    hints: ["Method calls use the same dot notation as accessing a field, but with parentheses at the end.", "calc.add()"],
    tags: ["classes", "methods", "oop"],
  },
  {
    id: "cc-cls-5",
    title: "A setter method that validates input",
    topic: "classes",
    difficulty: "advanced-beginner",
    scenario: "A BankAccount class's setBalance method should reject negative values rather than silently accepting them.",
    task: "Complete the guard condition inside setBalance.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nclass BankAccount {\nprivate:\n    double balance = 0;\npublic:\n    void setBalance(double amount) {\n        if (/*BLANK*/) {\n            balance = amount;\n        }\n    }\n    double getBalance() { return balance; }\n};\n\nint main() {\n    BankAccount acc;\n    acc.setBalance(-100);\n    cout << acc.getBalance();\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["amount >= 0"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nclass BankAccount {\nprivate:\n    double balance = 0;\npublic:\n    void setBalance(double amount) {\n        if (amount >= 0) {\n            balance = amount;\n        }\n    }\n    double getBalance() { return balance; }\n};\n\nint main() {\n    BankAccount acc;\n    acc.setBalance(-100);\n    cout << acc.getBalance();\n    return 0;\n}",
    testCases: ["acc.setBalance(-100) is rejected -> getBalance() still returns 0 (unchanged)"],
    explanation: "A setter method that validates its input before assigning is a core encapsulation benefit — because balance is private, the ONLY way to change it is through setBalance, which can enforce rules (like rejecting negative values) that direct field access could never guarantee.",
    commonMistake: "Making balance public instead of using a validated setter — this would let any code anywhere set an invalid negative balance directly, bypassing any validation entirely.",
    hints: ["Only accept amounts that aren't negative.", "amount >= 0"],
    tags: ["classes", "encapsulation", "validation"],
  },
  {
    id: "cc-cls-6",
    title: "Creating an object (instantiation)",
    topic: "classes",
    difficulty: "beginner",
    scenario: "A program needs to create an actual object of the Car class to work with.",
    task: "Complete the object declaration/instantiation.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nclass Car {\npublic:\n    string model = \"Corolla\";\n};\n\nint main() {\n    Car /*BLANK*/;\n    cout << myCar.model;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["myCar"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nclass Car {\npublic:\n    string model = \"Corolla\";\n};\n\nint main() {\n    Car myCar;\n    cout << myCar.model;\n    return 0;\n}",
    testCases: ["Output: Corolla"],
    explanation: "Car myCar; creates (instantiates) an actual object of type Car named myCar — a class is just a blueprint until you create a real instance of it, similar to how int x; creates an actual int variable from the int type.",
    commonMistake: "Confusing the class itself (Car, the type/blueprint) with an object of that class (myCar, an actual instance) — you can't access .model on Car directly; you need an actual object like myCar first.",
    hints: ["This is just the object's name, matching how it's used below (myCar.model).", "myCar"],
    tags: ["classes", "instantiation", "oop"],
  },
  {
    id: "cc-cls-7",
    title: "A method that modifies the object's own state",
    topic: "classes",
    difficulty: "intermediate",
    scenario: "A BankAccount class needs a deposit() method that increases its own balance field.",
    task: "Complete the deposit method body.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nclass BankAccount {\npublic:\n    double balance = 100;\n    void deposit(double amount) {\n        /*BLANK*/;\n    }\n};\n\nint main() {\n    BankAccount acc;\n    acc.deposit(50);\n    cout << acc.balance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["balance += amount"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nclass BankAccount {\npublic:\n    double balance = 100;\n    void deposit(double amount) {\n        balance += amount;\n    }\n};\n\nint main() {\n    BankAccount acc;\n    acc.deposit(50);\n    cout << acc.balance;\n    return 0;\n}",
    testCases: ["acc.deposit(50) -> acc.balance becomes 150"],
    explanation: "Inside a member function, a class's own fields (like balance) can be referred to directly by name, without needing an object prefix — balance += amount modifies THIS object's own balance field, whichever object the method happens to be called on.",
    commonMistake: "Writing 'acc.balance += amount;' inside the method itself — inside a member function you refer to your own object's fields directly by name; there's no 'acc' in scope inside deposit() at all (that name only exists in main()).",
    hints: ["Inside a method, you can refer to the object's own fields directly by name, no object prefix needed.", "balance += amount;"],
    tags: ["classes", "methods", "state"],
  },
  {
    id: "cc-cls-8",
    title: "Multiple objects, independent state",
    topic: "classes",
    difficulty: "intermediate",
    scenario: "Two separate BankAccount objects should have completely independent balances — depositing into one shouldn't affect the other.",
    task: "Complete the second object's deposit call, targeting acc2 specifically (not acc1).",
    starterCode: "#include <iostream>\nusing namespace std;\n\nclass BankAccount {\npublic:\n    double balance = 0;\n    void deposit(double amount) { balance += amount; }\n};\n\nint main() {\n    BankAccount acc1, acc2;\n    acc1.deposit(100);\n    /*BLANK*/.deposit(500);\n    cout << acc1.balance << \" \" << acc2.balance;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["acc2"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nclass BankAccount {\npublic:\n    double balance = 0;\n    void deposit(double amount) { balance += amount; }\n};\n\nint main() {\n    BankAccount acc1, acc2;\n    acc1.deposit(100);\n    acc2.deposit(500);\n    cout << acc1.balance << \" \" << acc2.balance;\n    return 0;\n}",
    testCases: ["acc1.balance=100, acc2.balance=500 — each object keeps its own separate balance"],
    explanation: "Each object created from a class gets its own independent copy of the class's data members — acc1 and acc2 each have their own separate balance field, so depositing into one has zero effect on the other's balance.",
    commonMistake: "Assuming all objects of a class share the same data (like a shared/static variable would) — by default, every object's non-static fields are completely independent of every other object's.",
    hints: ["You need to call deposit on the second object, acc2, not acc1 again.", "acc2.deposit(500);"],
    tags: ["classes", "objects", "state"],
  },

  // ================= S. Type Conversion (4) =================
  {
    id: "cc-tc-1",
    title: "Explicit cast from double to int",
    topic: "type-conversion",
    difficulty: "intermediate",
    scenario: "A shopping app needs to know how many WHOLE items 47.8 currency units can buy at 10 units each, discarding any partial item.",
    task: "Complete the explicit cast that converts the double result to an int, truncating the decimal part.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double money = 47.8;\n    int itemsAffordable = /*BLANK*/(money / 10);\n    cout << itemsAffordable;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["(int)", "int"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    double money = 47.8;\n    int itemsAffordable = (int)(money / 10);\n    cout << itemsAffordable;\n    return 0;\n}",
    testCases: ["money=47.8 -> money/10 = 4.78 -> (int) truncates to 4"],
    explanation: "(int)(...) explicitly casts a double to an int, truncating (not rounding) any fractional part — 4.78 becomes 4, discarding the .78 entirely rather than rounding to the nearest whole number.",
    commonMistake: "Assuming a cast to int rounds to the nearest whole number — it doesn't; (int) always truncates toward zero, so 4.78 becomes 4, and even 4.99 would still become 4, not 5.",
    hints: ["This cast, written as a type name in parentheses right before the value, converts a double to an int by truncating.", "(int)(money / 10)"],
    tags: ["type-conversion", "casting", "truncation"],
  },
  {
    id: "cc-tc-2",
    title: "Implicit conversion in mixed arithmetic",
    topic: "type-conversion",
    difficulty: "intermediate",
    scenario: "A program computes an average by dividing an int total by an int count, but wants the true decimal result rather than a truncated one.",
    task: "Complete the expression, converting count to a double so the division isn't pure integer division.",
    starterCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int total = 17, count = 4;\n    double average = total / /*BLANK*/(count);\n    cout << average;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["double", "(double)"] }],
    solutionCode: "#include <iostream>\nusing namespace std;\n\nint main() {\n    int total = 17, count = 4;\n    double average = total / double(count);\n    cout << average;\n    return 0;\n}",
    testCases: ["total=17, count=4 -> average = 4.25 (not 4)"],
    explanation: "Casting count to double before the division forces the ENTIRE division to happen in floating point (since one operand is now a double), correctly producing 4.25 instead of the truncated integer-division result of 4.",
    commonMistake: "Casting the result AFTER the division instead of before it (e.g., double(total / count)) — by then, integer division has already happened and truncated the result to 4; casting an already-truncated 4 to double just gives 4.0, not 4.25.",
    hints: ["Cast count to double BEFORE the division happens, not after.", "total / double(count)"],
    tags: ["type-conversion", "implicit-conversion", "division"],
  },
  {
    id: "cc-tc-3",
    title: "Converting a string to a number",
    topic: "type-conversion",
    difficulty: "advanced-beginner",
    scenario: "A form reads an age as text input and needs to convert it into an actual int for numeric comparisons.",
    task: "Complete the function call that converts a string to an int.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string ageText = \"27\";\n    int age = /*BLANK*/(ageText);\n    cout << age + 1;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["stoi"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    string ageText = \"27\";\n    int age = stoi(ageText);\n    cout << age + 1;\n    return 0;\n}",
    testCases: ["ageText=\"27\" -> age = 27, age+1 = 28"],
    explanation: "stoi (string to int) parses a numeric string and returns its integer value — it's the standard way to convert user-entered text like \"27\" into a real int that supports arithmetic, since a string like \"27\" cannot be added to directly (age + 1 wouldn't compile or behave as expected on the raw string).",
    commonMistake: "Trying to use (int)ageText (a direct cast) to convert a string to a number — casting doesn't parse text; stoi (or similar conversion functions) is required to interpret the characters as a number.",
    hints: ["There's a specific standard-library function whose name reads as 'string to int'.", "stoi(ageText)"],
    tags: ["type-conversion", "stoi", "strings"],
  },
  {
    id: "cc-tc-4",
    title: "Converting a number to a string",
    topic: "type-conversion",
    difficulty: "advanced-beginner",
    scenario: "A receipt builder needs to combine a numeric total with surrounding text into one string, which requires converting the number to text first.",
    task: "Complete the function call that converts total (a double) into a string.",
    starterCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    double total = 49.99;\n    string message = \"Total: \" + /*BLANK*/(total);\n    cout << message;\n    return 0;\n}",
    blanks: [{ id: "/*BLANK*/", kind: "inline", acceptableAnswers: ["to_string"] }],
    solutionCode: "#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    double total = 49.99;\n    string message = \"Total: \" + to_string(total);\n    cout << message;\n    return 0;\n}",
    testCases: ["total=49.99 -> message = \"Total: 49.990000\" (to_string uses a fixed default precision for doubles)"],
    explanation: "to_string(total) converts a numeric value into its string representation, which can then be concatenated with other strings using + — without this conversion, trying to + a string literal directly with a double would not compile, since + isn't defined between those two mismatched types.",
    commonMistake: "Trying to write \"Total: \" + total directly (without to_string) — the + operator has no built-in meaning for combining a string literal with a raw double, so this fails to compile.",
    hints: ["There's a standard-library function whose name reads as 'convert to string'.", "to_string(total)"],
    tags: ["type-conversion", "to_string", "strings"],
  },
];
